naftiko: 1.0.0-alpha2
info:
  label: IXON API — CompanyTermsOfUsePolicyLanguage
  description: 'IXON API — CompanyTermsOfUsePolicyLanguage. 1 operations. Lead operation: CompanyTermsOfUsePolicyLanguage.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - CompanyTermsOfUsePolicyLanguage
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-companytermsofusepolicylanguage
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — CompanyTermsOfUsePolicyLanguage business capability. Self-contained, no shared references.
    resources:
    - name: companies-terms-of-use-policy-languages
      path: /companies/terms-of-use-policy-languages
      operations:
      - name: patch
        method: PATCH
        description: CompanyTermsOfUsePolicyLanguage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-companytermsofusepolicylanguage-rest
    port: 8080
    description: REST adapter for IXON API — CompanyTermsOfUsePolicyLanguage. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/companies/terms-of-use-policy-languages
      name: companies-terms-of-use-policy-languages
      description: REST surface for companies-terms-of-use-policy-languages.
      operations:
      - method: PATCH
        name: patch
        description: CompanyTermsOfUsePolicyLanguage
        call: ixon-companytermsofusepolicylanguage.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-companytermsofusepolicylanguage-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — CompanyTermsOfUsePolicyLanguage. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: companytermsofusepolicylanguage
      description: CompanyTermsOfUsePolicyLanguage
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-companytermsofusepolicylanguage.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
