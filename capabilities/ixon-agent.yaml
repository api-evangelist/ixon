naftiko: 1.0.0-alpha2
info:
  label: IXON API — Agent
  description: 'IXON API — Agent. 3 operations. Lead operation: Agent. Self-contained Naftiko capability covering one Ixon
    business surface.'
  tags:
  - Ixon
  - Agent
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agent
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — Agent business capability. Self-contained, no shared references.
    resources:
    - name: agents-publicId
      path: /agents/{publicId}
      operations:
      - name: get
        method: GET
        description: Agent
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: Agent
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Agent
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agent-rest
    port: 8080
    description: REST adapter for IXON API — Agent. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/agents/{publicid}
      name: agents-publicid
      description: REST surface for agents-publicId.
      operations:
      - method: GET
        name: get
        description: Agent
        call: ixon-agent.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Agent
        call: ixon-agent.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Agent
        call: ixon-agent.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agent-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — Agent. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agent
      description: Agent
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agent.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agent-2
      description: Agent
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agent.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agent-3
      description: Agent
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agent.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
