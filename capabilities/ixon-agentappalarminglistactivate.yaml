naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAppAlarmingListActivate
  description: 'IXON API — AgentAppAlarmingListActivate. 1 operations. Lead operation: AgentAppAlarmingListActivate. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAppAlarmingListActivate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentappalarminglistactivate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAppAlarmingListActivate business capability. Self-contained, no shared references.
    resources:
    - name: agents-app-alarming-activate
      path: /agents/app/alarming/activate
      operations:
      - name: post
        method: POST
        description: AgentAppAlarmingListActivate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentappalarminglistactivate-rest
    port: 8080
    description: REST adapter for IXON API — AgentAppAlarmingListActivate. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/app/alarming/activate
      name: agents-app-alarming-activate
      description: REST surface for agents-app-alarming-activate.
      operations:
      - method: POST
        name: post
        description: AgentAppAlarmingListActivate
        call: ixon-agentappalarminglistactivate.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentappalarminglistactivate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAppAlarmingListActivate. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentappalarminglistactivate
      description: AgentAppAlarmingListActivate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentappalarminglistactivate.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
