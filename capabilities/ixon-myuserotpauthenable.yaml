naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserOtpAuthEnable
  description: 'IXON API — MyUserOtpAuthEnable. 1 operations. Lead operation: MyUserOtpAuthEnable. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserOtpAuthEnable
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserotpauthenable
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserOtpAuthEnable business capability. Self-contained, no shared references.
    resources:
    - name: users-me-otp-auth-enable
      path: /users/me/otp-auth/enable
      operations:
      - name: post
        method: POST
        description: MyUserOtpAuthEnable
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserotpauthenable-rest
    port: 8080
    description: REST adapter for IXON API — MyUserOtpAuthEnable. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/otp-auth/enable
      name: users-me-otp-auth-enable
      description: REST surface for users-me-otp-auth-enable.
      operations:
      - method: POST
        name: post
        description: MyUserOtpAuthEnable
        call: ixon-myuserotpauthenable.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserotpauthenable-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserOtpAuthEnable. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myuserotpauthenable
      description: MyUserOtpAuthEnable
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserotpauthenable.post
      outputParameters:
      - type: object
        mapping: $.
