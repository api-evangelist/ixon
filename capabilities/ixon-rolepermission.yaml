naftiko: 1.0.0-alpha2
info:
  label: IXON API — RolePermission
  description: 'IXON API — RolePermission. 1 operations. Lead operation: RolePermission. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - RolePermission
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-rolepermission
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — RolePermission business capability. Self-contained, no shared references.
    resources:
    - name: roles-parentId-permissions-publicId
      path: /roles/{parentId}/permissions/{publicId}
      operations:
      - name: delete
        method: DELETE
        description: RolePermission
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: parentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-rolepermission-rest
    port: 8080
    description: REST adapter for IXON API — RolePermission. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/roles/{parentid}/permissions/{publicid}
      name: roles-parentid-permissions-publicid
      description: REST surface for roles-parentId-permissions-publicId.
      operations:
      - method: DELETE
        name: delete
        description: RolePermission
        call: ixon-rolepermission.delete
        with:
          parentId: rest.parentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-rolepermission-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — RolePermission. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: rolepermission
      description: RolePermission
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-rolepermission.delete
      with:
        parentId: tools.parentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
