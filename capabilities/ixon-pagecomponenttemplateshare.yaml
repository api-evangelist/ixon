naftiko: 1.0.0-alpha2
info:
  label: IXON API — PageComponentTemplateShare
  description: 'IXON API — PageComponentTemplateShare. 1 operations. Lead operation: PageComponentTemplateShare. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PageComponentTemplateShare
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pagecomponenttemplateshare
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PageComponentTemplateShare business capability. Self-contained, no shared references.
    resources:
    - name: page-component-template-shares-publicId
      path: /page-component-template-shares/{publicId}
      operations:
      - name: get
        method: GET
        description: PageComponentTemplateShare
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pagecomponenttemplateshare-rest
    port: 8080
    description: REST adapter for IXON API — PageComponentTemplateShare. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/page-component-template-shares/{publicid}
      name: page-component-template-shares-publicid
      description: REST surface for page-component-template-shares-publicId.
      operations:
      - method: GET
        name: get
        description: PageComponentTemplateShare
        call: ixon-pagecomponenttemplateshare.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pagecomponenttemplateshare-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PageComponentTemplateShare. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pagecomponenttemplateshare
      description: PageComponentTemplateShare
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-pagecomponenttemplateshare.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
