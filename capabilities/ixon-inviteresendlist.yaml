naftiko: 1.0.0-alpha2
info:
  label: IXON API — InviteResendList
  description: 'IXON API — InviteResendList. 1 operations. Lead operation: InviteResendList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - InviteResendList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-inviteresendlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — InviteResendList business capability. Self-contained, no shared references.
    resources:
    - name: invites-resend
      path: /invites/resend
      operations:
      - name: post
        method: POST
        description: InviteResendList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-inviteresendlist-rest
    port: 8080
    description: REST adapter for IXON API — InviteResendList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/invites/resend
      name: invites-resend
      description: REST surface for invites-resend.
      operations:
      - method: POST
        name: post
        description: InviteResendList
        call: ixon-inviteresendlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-inviteresendlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — InviteResendList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: inviteresendlist
      description: InviteResendList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-inviteresendlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
