naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentList
  description: 'IXON API — AgentList. 3 operations. Lead operation: AgentList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentList business capability. Self-contained, no shared references.
    resources:
    - name: agents
      path: /agents
      operations:
      - name: get
        method: GET
        description: AgentList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: AgentList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentList. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/agents
      name: agents
      description: REST surface for agents.
      operations:
      - method: GET
        name: get
        description: AgentList
        call: ixon-agentlist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentList
        call: ixon-agentlist.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentList
        call: ixon-agentlist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agentlist
      description: AgentList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentlist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: agentlist-2
      description: AgentList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentlist.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentlist-3
      description: AgentList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentlist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
