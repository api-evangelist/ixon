naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentOpenVpnConnection
  description: 'IXON API — AgentOpenVpnConnection. 1 operations. Lead operation: AgentOpenVpnConnection. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentOpenVpnConnection
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentopenvpnconnection
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentOpenVpnConnection business capability. Self-contained, no shared references.
    resources:
    - name: agents-publicId-connection-openvpn
      path: /agents/{publicId}/connection/openvpn
      operations:
      - name: post
        method: POST
        description: AgentOpenVpnConnection
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentopenvpnconnection-rest
    port: 8080
    description: REST adapter for IXON API — AgentOpenVpnConnection. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{publicid}/connection/openvpn
      name: agents-publicid-connection-openvpn
      description: REST surface for agents-publicId-connection-openvpn.
      operations:
      - method: POST
        name: post
        description: AgentOpenVpnConnection
        call: ixon-agentopenvpnconnection.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentopenvpnconnection-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentOpenVpnConnection. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentopenvpnconnection
      description: AgentOpenVpnConnection
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentopenvpnconnection.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
