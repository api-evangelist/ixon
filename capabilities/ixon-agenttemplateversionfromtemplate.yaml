naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentTemplateVersionFromTemplate
  description: 'IXON API — AgentTemplateVersionFromTemplate. 1 operations. Lead operation: AgentTemplateVersionFromTemplate.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentTemplateVersionFromTemplate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agenttemplateversionfromtemplate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentTemplateVersionFromTemplate business capability. Self-contained, no shared references.
    resources:
    - name: agent-templates-publicId-version
      path: /agent-templates/{publicId}/version
      operations:
      - name: post
        method: POST
        description: AgentTemplateVersionFromTemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agenttemplateversionfromtemplate-rest
    port: 8080
    description: REST adapter for IXON API — AgentTemplateVersionFromTemplate. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agent-templates/{publicid}/version
      name: agent-templates-publicid-version
      description: REST surface for agent-templates-publicId-version.
      operations:
      - method: POST
        name: post
        description: AgentTemplateVersionFromTemplate
        call: ixon-agenttemplateversionfromtemplate.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agenttemplateversionfromtemplate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentTemplateVersionFromTemplate. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agenttemplateversionfromtemplate
      description: AgentTemplateVersionFromTemplate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agenttemplateversionfromtemplate.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
