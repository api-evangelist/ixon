naftiko: 1.0.0-alpha2
info:
  label: IXON API — InviteResend
  description: 'IXON API — InviteResend. 1 operations. Lead operation: InviteResend. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - InviteResend
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-inviteresend
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — InviteResend business capability. Self-contained, no shared references.
    resources:
    - name: invites-publicId-resend
      path: /invites/{publicId}/resend
      operations:
      - name: post
        method: POST
        description: InviteResend
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-inviteresend-rest
    port: 8080
    description: REST adapter for IXON API — InviteResend. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/invites/{publicid}/resend
      name: invites-publicid-resend
      description: REST surface for invites-publicId-resend.
      operations:
      - method: POST
        name: post
        description: InviteResend
        call: ixon-inviteresend.post
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-inviteresend-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — InviteResend. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: inviteresend
      description: InviteResend
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-inviteresend.post
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
