naftiko: 1.0.0-alpha2
info:
  label: IXON API — OciRegistry
  description: 'IXON API — OciRegistry. 3 operations. Lead operation: OciRegistry. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - OciRegistry
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-ociregistry
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — OciRegistry business capability. Self-contained, no shared references.
    resources:
    - name: oci-registries-publicId
      path: /oci-registries/{publicId}
      operations:
      - name: get
        method: GET
        description: OciRegistry
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: OciRegistry
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: OciRegistry
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-ociregistry-rest
    port: 8080
    description: REST adapter for IXON API — OciRegistry. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/oci-registries/{publicid}
      name: oci-registries-publicid
      description: REST surface for oci-registries-publicId.
      operations:
      - method: GET
        name: get
        description: OciRegistry
        call: ixon-ociregistry.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: OciRegistry
        call: ixon-ociregistry.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: OciRegistry
        call: ixon-ociregistry.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-ociregistry-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — OciRegistry. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: ociregistry
      description: OciRegistry
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-ociregistry.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: ociregistry-2
      description: OciRegistry
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-ociregistry.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: ociregistry-3
      description: OciRegistry
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-ociregistry.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
