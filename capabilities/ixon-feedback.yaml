naftiko: 1.0.0-alpha2
info:
  label: IXON API — Feedback
  description: 'IXON API — Feedback. 1 operations. Lead operation: Feedback. Self-contained Naftiko capability covering one
    Ixon business surface.'
  tags:
  - Ixon
  - Feedback
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-feedback
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — Feedback business capability. Self-contained, no shared references.
    resources:
    - name: feedback
      path: /feedback
      operations:
      - name: post
        method: POST
        description: Feedback
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-feedback-rest
    port: 8080
    description: REST adapter for IXON API — Feedback. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/feedback
      name: feedback
      description: REST surface for feedback.
      operations:
      - method: POST
        name: post
        description: Feedback
        call: ixon-feedback.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-feedback-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — Feedback. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: feedback
      description: Feedback
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-feedback.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
