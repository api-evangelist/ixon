naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentPageLinkGroup
  description: 'IXON API — AgentPageLinkGroup. 3 operations. Lead operation: AgentPageLinkGroup. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentPageLinkGroup
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentpagelinkgroup
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentPageLinkGroup business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-page-link-groups-publicId
      path: /agents/{agentId}/page-link-groups/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentPageLinkGroup
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentPageLinkGroup
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentPageLinkGroup
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentpagelinkgroup-rest
    port: 8080
    description: REST adapter for IXON API — AgentPageLinkGroup. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/page-link-groups/{publicid}
      name: agents-agentid-page-link-groups-publicid
      description: REST surface for agents-agentId-page-link-groups-publicId.
      operations:
      - method: GET
        name: get
        description: AgentPageLinkGroup
        call: ixon-agentpagelinkgroup.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentPageLinkGroup
        call: ixon-agentpagelinkgroup.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentPageLinkGroup
        call: ixon-agentpagelinkgroup.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentpagelinkgroup-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentPageLinkGroup. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentpagelinkgroup
      description: AgentPageLinkGroup
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentpagelinkgroup.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentpagelinkgroup-2
      description: AgentPageLinkGroup
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentpagelinkgroup.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentpagelinkgroup-3
      description: AgentPageLinkGroup
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentpagelinkgroup.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
