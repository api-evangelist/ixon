naftiko: 1.0.0-alpha2
info:
  label: IXON API — CompanyBranding
  description: 'IXON API — CompanyBranding. 1 operations. Lead operation: CompanyBranding. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - CompanyBranding
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-companybranding
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — CompanyBranding business capability. Self-contained, no shared references.
    resources:
    - name: companies-publicId-branding
      path: /companies/{publicId}/branding
      operations:
      - name: get
        method: GET
        description: CompanyBranding
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-companybranding-rest
    port: 8080
    description: REST adapter for IXON API — CompanyBranding. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/companies/{publicid}/branding
      name: companies-publicid-branding
      description: REST surface for companies-publicId-branding.
      operations:
      - method: GET
        name: get
        description: CompanyBranding
        call: ixon-companybranding.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-companybranding-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — CompanyBranding. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: companybranding
      description: CompanyBranding
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-companybranding.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
