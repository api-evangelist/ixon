naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetTreeList
  description: 'IXON API — AssetTreeList. 1 operations. Lead operation: AssetTreeList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AssetTreeList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assettreelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetTreeList business capability. Self-contained, no shared references.
    resources:
    - name: asset-trees
      path: /asset-trees
      operations:
      - name: put
        method: PUT
        description: AssetTreeList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assettreelist-rest
    port: 8080
    description: REST adapter for IXON API — AssetTreeList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/asset-trees
      name: asset-trees
      description: REST surface for asset-trees.
      operations:
      - method: PUT
        name: put
        description: AssetTreeList
        call: ixon-assettreelist.put
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assettreelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetTreeList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: assettreelist
      description: AssetTreeList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-assettreelist.put
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
