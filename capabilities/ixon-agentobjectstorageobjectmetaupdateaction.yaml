naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentObjectStorageObjectMetaUpdateAction
  description: 'IXON API — AgentObjectStorageObjectMetaUpdateAction. 1 operations. Lead operation: AgentObjectStorageObjectMetaUpdateAction.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentObjectStorageObjectMetaUpdateAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentobjectstorageobjectmetaupdateaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentObjectStorageObjectMetaUpdateAction business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-object-storage-meta
      path: /agents/{agentId}/object-storage/meta
      operations:
      - name: patch
        method: PATCH
        description: AgentObjectStorageObjectMetaUpdateAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentobjectstorageobjectmetaupdateaction-rest
    port: 8080
    description: REST adapter for IXON API — AgentObjectStorageObjectMetaUpdateAction. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/object-storage/meta
      name: agents-agentid-object-storage-meta
      description: REST surface for agents-agentId-object-storage-meta.
      operations:
      - method: PATCH
        name: patch
        description: AgentObjectStorageObjectMetaUpdateAction
        call: ixon-agentobjectstorageobjectmetaupdateaction.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentobjectstorageobjectmetaupdateaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentObjectStorageObjectMetaUpdateAction. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: agentobjectstorageobjectmetaupdateaction
      description: AgentObjectStorageObjectMetaUpdateAction
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentobjectstorageobjectmetaupdateaction.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
