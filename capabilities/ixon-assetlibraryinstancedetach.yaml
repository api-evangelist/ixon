naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetLibraryInstanceDetach
  description: 'IXON API — AssetLibraryInstanceDetach. 1 operations. Lead operation: AssetLibraryInstanceDetach. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetLibraryInstanceDetach
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetlibraryinstancedetach
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetLibraryInstanceDetach business capability. Self-contained, no shared references.
    resources:
    - name: assets-library-instances-publicId-detach
      path: /assets/library/instances/{publicId}/detach
      operations:
      - name: patch
        method: PATCH
        description: AssetLibraryInstanceDetach
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetlibraryinstancedetach-rest
    port: 8080
    description: REST adapter for IXON API — AssetLibraryInstanceDetach. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/assets/library/instances/{publicid}/detach
      name: assets-library-instances-publicid-detach
      description: REST surface for assets-library-instances-publicId-detach.
      operations:
      - method: PATCH
        name: patch
        description: AssetLibraryInstanceDetach
        call: ixon-assetlibraryinstancedetach.patch
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetlibraryinstancedetach-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetLibraryInstanceDetach. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: assetlibraryinstancedetach
      description: AssetLibraryInstanceDetach
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-assetlibraryinstancedetach.patch
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
