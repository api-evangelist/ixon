naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceList
  description: 'IXON API — AgentEdgeAppInstanceList. 3 operations. Lead operation: AgentEdgeAppInstanceList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstancelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances
      path: /agents/{agentId}/edge-app-instances
      operations:
      - name: get
        method: GET
        description: AgentEdgeAppInstanceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
      - name: post
        method: POST
        description: AgentEdgeAppInstanceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patch
        method: PATCH
        description: AgentEdgeAppInstanceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstancelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances
      name: agents-agentid-edge-app-instances
      description: REST surface for agents-agentId-edge-app-instances.
      operations:
      - method: GET
        name: get
        description: AgentEdgeAppInstanceList
        call: ixon-agentedgeappinstancelist.get
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: AgentEdgeAppInstanceList
        call: ixon-agentedgeappinstancelist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentEdgeAppInstanceList
        call: ixon-agentedgeappinstancelist.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstancelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentedgeappinstancelist
      description: AgentEdgeAppInstanceList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentedgeappinstancelist.get
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentedgeappinstancelist-2
      description: AgentEdgeAppInstanceList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentedgeappinstancelist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentedgeappinstancelist-3
      description: AgentEdgeAppInstanceList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentedgeappinstancelist.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
