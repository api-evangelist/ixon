naftiko: 1.0.0-alpha2
info:
  label: IXON API — TermsOfUsePolicyDownload
  description: 'IXON API — TermsOfUsePolicyDownload. 1 operations. Lead operation: TermsOfUsePolicyDownload. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - TermsOfUsePolicyDownload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-termsofusepolicydownload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — TermsOfUsePolicyDownload business capability. Self-contained, no shared references.
    resources:
    - name: terms-of-use-policies-publicId-download
      path: /terms-of-use-policies/{publicId}/download
      operations:
      - name: get
        method: GET
        description: TermsOfUsePolicyDownload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-termsofusepolicydownload-rest
    port: 8080
    description: REST adapter for IXON API — TermsOfUsePolicyDownload. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/terms-of-use-policies/{publicid}/download
      name: terms-of-use-policies-publicid-download
      description: REST surface for terms-of-use-policies-publicId-download.
      operations:
      - method: GET
        name: get
        description: TermsOfUsePolicyDownload
        call: ixon-termsofusepolicydownload.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-termsofusepolicydownload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — TermsOfUsePolicyDownload. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: termsofusepolicydownload
      description: TermsOfUsePolicyDownload
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-termsofusepolicydownload.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
