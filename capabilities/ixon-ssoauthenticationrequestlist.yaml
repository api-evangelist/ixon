naftiko: 1.0.0-alpha2
info:
  label: IXON API — SsoAuthenticationRequestList
  description: 'IXON API — SsoAuthenticationRequestList. 1 operations. Lead operation: SsoAuthenticationRequestList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - SsoAuthenticationRequestList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-ssoauthenticationrequestlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — SsoAuthenticationRequestList business capability. Self-contained, no shared references.
    resources:
    - name: sso-authentication-request
      path: /sso/authentication/request
      operations:
      - name: post
        method: POST
        description: SsoAuthenticationRequestList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-ssoauthenticationrequestlist-rest
    port: 8080
    description: REST adapter for IXON API — SsoAuthenticationRequestList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/sso/authentication/request
      name: sso-authentication-request
      description: REST surface for sso-authentication-request.
      operations:
      - method: POST
        name: post
        description: SsoAuthenticationRequestList
        call: ixon-ssoauthenticationrequestlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-ssoauthenticationrequestlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — SsoAuthenticationRequestList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: ssoauthenticationrequestlist
      description: SsoAuthenticationRequestList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-ssoauthenticationrequestlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
