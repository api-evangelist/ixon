naftiko: 1.0.0-alpha2
info:
  label: IXON API — LicenceTypeList
  description: 'IXON API — LicenceTypeList. 1 operations. Lead operation: LicenceTypeList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - LicenceTypeList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-licencetypelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — LicenceTypeList business capability. Self-contained, no shared references.
    resources:
    - name: licence-types
      path: /licence-types
      operations:
      - name: get
        method: GET
        description: LicenceTypeList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-licencetypelist-rest
    port: 8080
    description: REST adapter for IXON API — LicenceTypeList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/licence-types
      name: licence-types
      description: REST surface for licence-types.
      operations:
      - method: GET
        name: get
        description: LicenceTypeList
        call: ixon-licencetypelist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-licencetypelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — LicenceTypeList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: licencetypelist
      description: LicenceTypeList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-licencetypelist.get
      outputParameters:
      - type: object
        mapping: $.
