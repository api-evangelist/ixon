naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentDataAlarmOccurrence
  description: 'IXON API — AgentDataAlarmOccurrence. 2 operations. Lead operation: AgentDataAlarmOccurrence. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentDataAlarmOccurrence
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentdataalarmoccurrence
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentDataAlarmOccurrence business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-alarm-occurrences-publicId
      path: /agents/{agentId}/alarm-occurrences/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentDataAlarmOccurrence
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentDataAlarmOccurrence
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentdataalarmoccurrence-rest
    port: 8080
    description: REST adapter for IXON API — AgentDataAlarmOccurrence. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/alarm-occurrences/{publicid}
      name: agents-agentid-alarm-occurrences-publicid
      description: REST surface for agents-agentId-alarm-occurrences-publicId.
      operations:
      - method: GET
        name: get
        description: AgentDataAlarmOccurrence
        call: ixon-agentdataalarmoccurrence.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentDataAlarmOccurrence
        call: ixon-agentdataalarmoccurrence.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentdataalarmoccurrence-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentDataAlarmOccurrence. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentdataalarmoccurrence
      description: AgentDataAlarmOccurrence
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentdataalarmoccurrence.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdataalarmoccurrence-2
      description: AgentDataAlarmOccurrence
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentdataalarmoccurrence.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
