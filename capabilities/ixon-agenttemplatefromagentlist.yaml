naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentTemplateFromAgentList
  description: 'IXON API — AgentTemplateFromAgentList. 1 operations. Lead operation: AgentTemplateFromAgentList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentTemplateFromAgentList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agenttemplatefromagentlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentTemplateFromAgentList business capability. Self-contained, no shared references.
    resources:
    - name: agent-templates-from-agent
      path: /agent-templates/from-agent
      operations:
      - name: post
        method: POST
        description: AgentTemplateFromAgentList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agenttemplatefromagentlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentTemplateFromAgentList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agent-templates/from-agent
      name: agent-templates-from-agent
      description: REST surface for agent-templates-from-agent.
      operations:
      - method: POST
        name: post
        description: AgentTemplateFromAgentList
        call: ixon-agenttemplatefromagentlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agenttemplatefromagentlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentTemplateFromAgentList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agenttemplatefromagentlist
      description: AgentTemplateFromAgentList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agenttemplatefromagentlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
