naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentObjectStorageListAction
  description: 'IXON API — AgentObjectStorageListAction. 1 operations. Lead operation: AgentObjectStorageListAction. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentObjectStorageListAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentobjectstoragelistaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentObjectStorageListAction business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-object-storage-list
      path: /agents/{agentId}/object-storage/list
      operations:
      - name: post
        method: POST
        description: AgentObjectStorageListAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentobjectstoragelistaction-rest
    port: 8080
    description: REST adapter for IXON API — AgentObjectStorageListAction. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/object-storage/list
      name: agents-agentid-object-storage-list
      description: REST surface for agents-agentId-object-storage-list.
      operations:
      - method: POST
        name: post
        description: AgentObjectStorageListAction
        call: ixon-agentobjectstoragelistaction.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentobjectstoragelistaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentObjectStorageListAction. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentobjectstoragelistaction
      description: AgentObjectStorageListAction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentobjectstoragelistaction.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
