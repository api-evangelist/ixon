naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserPassword
  description: 'IXON API — MyUserPassword. 1 operations. Lead operation: MyUserPassword. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserPassword
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserpassword
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserPassword business capability. Self-contained, no shared references.
    resources:
    - name: users-me-password
      path: /users/me/password
      operations:
      - name: post
        method: POST
        description: MyUserPassword
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserpassword-rest
    port: 8080
    description: REST adapter for IXON API — MyUserPassword. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/me/password
      name: users-me-password
      description: REST surface for users-me-password.
      operations:
      - method: POST
        name: post
        description: MyUserPassword
        call: ixon-myuserpassword.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserpassword-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserPassword. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: myuserpassword
      description: MyUserPassword
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserpassword.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
