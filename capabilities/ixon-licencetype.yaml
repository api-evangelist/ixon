naftiko: 1.0.0-alpha2
info:
  label: IXON API — LicenceType
  description: 'IXON API — LicenceType. 1 operations. Lead operation: LicenceType. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - LicenceType
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-licencetype
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — LicenceType business capability. Self-contained, no shared references.
    resources:
    - name: licence-types-publicId
      path: /licence-types/{publicId}
      operations:
      - name: get
        method: GET
        description: LicenceType
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-licencetype-rest
    port: 8080
    description: REST adapter for IXON API — LicenceType. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/licence-types/{publicid}
      name: licence-types-publicid
      description: REST surface for licence-types-publicId.
      operations:
      - method: GET
        name: get
        description: LicenceType
        call: ixon-licencetype.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-licencetype-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — LicenceType. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: licencetype
      description: LicenceType
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-licencetype.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
