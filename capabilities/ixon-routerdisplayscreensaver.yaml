naftiko: 1.0.0-alpha2
info:
  label: IXON API — RouterDisplayScreensaver
  description: 'IXON API — RouterDisplayScreensaver. 2 operations. Lead operation: RouterDisplayScreensaver. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - RouterDisplayScreensaver
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-routerdisplayscreensaver
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — RouterDisplayScreensaver business capability. Self-contained, no shared references.
    resources:
    - name: router-display-screensaver-publicId
      path: /router-display-screensaver/{publicId}
      operations:
      - name: get
        method: GET
        description: RouterDisplayScreensaver
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: delete
        method: DELETE
        description: RouterDisplayScreensaver
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-routerdisplayscreensaver-rest
    port: 8080
    description: REST adapter for IXON API — RouterDisplayScreensaver. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/router-display-screensaver/{publicid}
      name: router-display-screensaver-publicid
      description: REST surface for router-display-screensaver-publicId.
      operations:
      - method: GET
        name: get
        description: RouterDisplayScreensaver
        call: ixon-routerdisplayscreensaver.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: RouterDisplayScreensaver
        call: ixon-routerdisplayscreensaver.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-routerdisplayscreensaver-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — RouterDisplayScreensaver. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: routerdisplayscreensaver
      description: RouterDisplayScreensaver
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-routerdisplayscreensaver.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: routerdisplayscreensaver-2
      description: RouterDisplayScreensaver
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-routerdisplayscreensaver.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
