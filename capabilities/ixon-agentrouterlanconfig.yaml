naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterLanConfig
  description: 'IXON API — AgentRouterLanConfig. 1 operations. Lead operation: AgentRouterLanConfig. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterLanConfig
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentrouterlanconfig
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterLanConfig business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-lan-config
      path: /agents/{agentId}/router-lan-config
      operations:
      - name: patch
        method: PATCH
        description: AgentRouterLanConfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentrouterlanconfig-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterLanConfig. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-lan-config
      name: agents-agentid-router-lan-config
      description: REST surface for agents-agentId-router-lan-config.
      operations:
      - method: PATCH
        name: patch
        description: AgentRouterLanConfig
        call: ixon-agentrouterlanconfig.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentrouterlanconfig-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterLanConfig. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentrouterlanconfig
      description: AgentRouterLanConfig
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentrouterlanconfig.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
