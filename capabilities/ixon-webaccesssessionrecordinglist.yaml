naftiko: 1.0.0-alpha2
info:
  label: IXON API — WebaccessSessionRecordingList
  description: 'IXON API — WebaccessSessionRecordingList. 1 operations. Lead operation: WebaccessSessionRecordingList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - WebaccessSessionRecordingList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-webaccesssessionrecordinglist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — WebaccessSessionRecordingList business capability. Self-contained, no shared references.
    resources:
    - name: web-access-session-recordings
      path: /web-access/session-recordings
      operations:
      - name: get
        method: GET
        description: WebaccessSessionRecordingList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-webaccesssessionrecordinglist-rest
    port: 8080
    description: REST adapter for IXON API — WebaccessSessionRecordingList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/web-access/session-recordings
      name: web-access-session-recordings
      description: REST surface for web-access-session-recordings.
      operations:
      - method: GET
        name: get
        description: WebaccessSessionRecordingList
        call: ixon-webaccesssessionrecordinglist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-webaccesssessionrecordinglist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — WebaccessSessionRecordingList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: webaccesssessionrecordinglist
      description: WebaccessSessionRecordingList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-webaccesssessionrecordinglist.get
      outputParameters:
      - type: object
        mapping: $.
