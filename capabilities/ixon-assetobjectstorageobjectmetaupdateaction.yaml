naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetObjectStorageObjectMetaUpdateAction
  description: 'IXON API — AssetObjectStorageObjectMetaUpdateAction. 1 operations. Lead operation: AssetObjectStorageObjectMetaUpdateAction.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetObjectStorageObjectMetaUpdateAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetobjectstorageobjectmetaupdateaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetObjectStorageObjectMetaUpdateAction business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetId-object-storage-meta
      path: /assets/{assetId}/object-storage/meta
      operations:
      - name: patch
        method: PATCH
        description: AssetObjectStorageObjectMetaUpdateAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assetId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetobjectstorageobjectmetaupdateaction-rest
    port: 8080
    description: REST adapter for IXON API — AssetObjectStorageObjectMetaUpdateAction. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/assets/{assetid}/object-storage/meta
      name: assets-assetid-object-storage-meta
      description: REST surface for assets-assetId-object-storage-meta.
      operations:
      - method: PATCH
        name: patch
        description: AssetObjectStorageObjectMetaUpdateAction
        call: ixon-assetobjectstorageobjectmetaupdateaction.patch
        with:
          assetId: rest.assetId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetobjectstorageobjectmetaupdateaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetObjectStorageObjectMetaUpdateAction. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: assetobjectstorageobjectmetaupdateaction
      description: AssetObjectStorageObjectMetaUpdateAction
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-assetobjectstorageobjectmetaupdateaction.patch
      with:
        assetId: tools.assetId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
