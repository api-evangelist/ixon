naftiko: 1.0.0-alpha2
info:
  label: IXON API — LicenceBundleExtend
  description: 'IXON API — LicenceBundleExtend. 1 operations. Lead operation: LicenceBundleExtend. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - LicenceBundleExtend
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-licencebundleextend
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — LicenceBundleExtend business capability. Self-contained, no shared references.
    resources:
    - name: licence-bundles-publicId-extend
      path: /licence-bundles/{publicId}/extend
      operations:
      - name: post
        method: POST
        description: LicenceBundleExtend
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-licencebundleextend-rest
    port: 8080
    description: REST adapter for IXON API — LicenceBundleExtend. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/licence-bundles/{publicid}/extend
      name: licence-bundles-publicid-extend
      description: REST surface for licence-bundles-publicId-extend.
      operations:
      - method: POST
        name: post
        description: LicenceBundleExtend
        call: ixon-licencebundleextend.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-licencebundleextend-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — LicenceBundleExtend. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: licencebundleextend
      description: LicenceBundleExtend
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-licencebundleextend.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
