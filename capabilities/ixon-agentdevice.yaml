naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentDevice
  description: 'IXON API — AgentDevice. 3 operations. Lead operation: AgentDevice. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentDevice
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentdevice
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentDevice business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-devices-publicId
      path: /agents/{agentId}/devices/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentDevice
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentDevice
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentDevice
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentdevice-rest
    port: 8080
    description: REST adapter for IXON API — AgentDevice. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/devices/{publicid}
      name: agents-agentid-devices-publicid
      description: REST surface for agents-agentId-devices-publicId.
      operations:
      - method: GET
        name: get
        description: AgentDevice
        call: ixon-agentdevice.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentDevice
        call: ixon-agentdevice.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentDevice
        call: ixon-agentdevice.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentdevice-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentDevice. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agentdevice
      description: AgentDevice
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentdevice.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdevice-2
      description: AgentDevice
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentdevice.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdevice-3
      description: AgentDevice
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentdevice.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
