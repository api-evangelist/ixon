naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserOtpAuthVerify
  description: 'IXON API — MyUserOtpAuthVerify. 1 operations. Lead operation: MyUserOtpAuthVerify. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserOtpAuthVerify
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserotpauthverify
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserOtpAuthVerify business capability. Self-contained, no shared references.
    resources:
    - name: users-me-otp-auth-verify
      path: /users/me/otp-auth/verify
      operations:
      - name: post
        method: POST
        description: MyUserOtpAuthVerify
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserotpauthverify-rest
    port: 8080
    description: REST adapter for IXON API — MyUserOtpAuthVerify. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/otp-auth/verify
      name: users-me-otp-auth-verify
      description: REST surface for users-me-otp-auth-verify.
      operations:
      - method: POST
        name: post
        description: MyUserOtpAuthVerify
        call: ixon-myuserotpauthverify.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserotpauthverify-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserOtpAuthVerify. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myuserotpauthverify
      description: MyUserOtpAuthVerify
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserotpauthverify.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
