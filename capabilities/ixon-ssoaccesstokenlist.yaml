naftiko: 1.0.0-alpha2
info:
  label: IXON API — SsoAccessTokenList
  description: 'IXON API — SsoAccessTokenList. 1 operations. Lead operation: SsoAccessTokenList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - SsoAccessTokenList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-ssoaccesstokenlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — SsoAccessTokenList business capability. Self-contained, no shared references.
    resources:
    - name: sso-access-token
      path: /sso/access-token
      operations:
      - name: post
        method: POST
        description: SsoAccessTokenList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-ssoaccesstokenlist-rest
    port: 8080
    description: REST adapter for IXON API — SsoAccessTokenList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/sso/access-token
      name: sso-access-token
      description: REST surface for sso-access-token.
      operations:
      - method: POST
        name: post
        description: SsoAccessTokenList
        call: ixon-ssoaccesstokenlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-ssoaccesstokenlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — SsoAccessTokenList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: ssoaccesstokenlist
      description: SsoAccessTokenList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-ssoaccesstokenlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
