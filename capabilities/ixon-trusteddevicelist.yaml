naftiko: 1.0.0-alpha2
info:
  label: IXON API — TrustedDeviceList
  description: 'IXON API — TrustedDeviceList. 2 operations. Lead operation: TrustedDeviceList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - TrustedDeviceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-trusteddevicelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — TrustedDeviceList business capability. Self-contained, no shared references.
    resources:
    - name: trusted-devices
      path: /trusted-devices
      operations:
      - name: get
        method: GET
        description: TrustedDeviceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: delete
        method: DELETE
        description: TrustedDeviceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-trusteddevicelist-rest
    port: 8080
    description: REST adapter for IXON API — TrustedDeviceList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/trusted-devices
      name: trusted-devices
      description: REST surface for trusted-devices.
      operations:
      - method: GET
        name: get
        description: TrustedDeviceList
        call: ixon-trusteddevicelist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: TrustedDeviceList
        call: ixon-trusteddevicelist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-trusteddevicelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — TrustedDeviceList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: trusteddevicelist
      description: TrustedDeviceList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-trusteddevicelist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: trusteddevicelist-2
      description: TrustedDeviceList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-trusteddevicelist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
