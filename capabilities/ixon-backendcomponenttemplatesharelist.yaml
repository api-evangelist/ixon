naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentTemplateShareList
  description: 'IXON API — BackendComponentTemplateShareList. 2 operations. Lead operation: BackendComponentTemplateShareList.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentTemplateShareList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponenttemplatesharelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentTemplateShareList business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-template-shares
      path: /backend-component-template-shares
      operations:
      - name: get
        method: GET
        description: BackendComponentTemplateShareList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: BackendComponentTemplateShareList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponenttemplatesharelist-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentTemplateShareList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/backend-component-template-shares
      name: backend-component-template-shares
      description: REST surface for backend-component-template-shares.
      operations:
      - method: GET
        name: get
        description: BackendComponentTemplateShareList
        call: ixon-backendcomponenttemplatesharelist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: BackendComponentTemplateShareList
        call: ixon-backendcomponenttemplatesharelist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponenttemplatesharelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentTemplateShareList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: backendcomponenttemplatesharelist
      description: BackendComponentTemplateShareList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-backendcomponenttemplatesharelist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: backendcomponenttemplatesharelist-2
      description: BackendComponentTemplateShareList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-backendcomponenttemplatesharelist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
