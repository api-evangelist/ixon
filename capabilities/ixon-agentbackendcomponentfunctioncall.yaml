naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentBackendComponentFunctionCall
  description: 'IXON API — AgentBackendComponentFunctionCall. 1 operations. Lead operation: AgentBackendComponentFunctionCall.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentBackendComponentFunctionCall
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentbackendcomponentfunctioncall
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentBackendComponentFunctionCall business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-backend-component-function-call
      path: /agents/{agentId}/backend-component-function-call
      operations:
      - name: post
        method: POST
        description: AgentBackendComponentFunctionCall
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentbackendcomponentfunctioncall-rest
    port: 8080
    description: REST adapter for IXON API — AgentBackendComponentFunctionCall. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/backend-component-function-call
      name: agents-agentid-backend-component-function-call
      description: REST surface for agents-agentId-backend-component-function-call.
      operations:
      - method: POST
        name: post
        description: AgentBackendComponentFunctionCall
        call: ixon-agentbackendcomponentfunctioncall.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentbackendcomponentfunctioncall-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentBackendComponentFunctionCall. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentbackendcomponentfunctioncall
      description: AgentBackendComponentFunctionCall
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentbackendcomponentfunctioncall.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
