naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetDescendantList
  description: 'IXON API — AssetDescendantList. 1 operations. Lead operation: AssetDescendantList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetDescendantList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetdescendantlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetDescendantList business capability. Self-contained, no shared references.
    resources:
    - name: assets-publicId-descendants
      path: /assets/{publicId}/descendants
      operations:
      - name: get
        method: GET
        description: AssetDescendantList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetdescendantlist-rest
    port: 8080
    description: REST adapter for IXON API — AssetDescendantList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/assets/{publicid}/descendants
      name: assets-publicid-descendants
      description: REST surface for assets-publicId-descendants.
      operations:
      - method: GET
        name: get
        description: AssetDescendantList
        call: ixon-assetdescendantlist.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetdescendantlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetDescendantList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: assetdescendantlist
      description: AssetDescendantList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-assetdescendantlist.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
