naftiko: 1.0.0-alpha2
info:
  label: IXON API — DemoDeviceList
  description: 'IXON API — DemoDeviceList. 1 operations. Lead operation: DemoDeviceList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - DemoDeviceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-demodevicelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — DemoDeviceList business capability. Self-contained, no shared references.
    resources:
    - name: demo-devices
      path: /demo-devices
      operations:
      - name: post
        method: POST
        description: DemoDeviceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-demodevicelist-rest
    port: 8080
    description: REST adapter for IXON API — DemoDeviceList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/demo-devices
      name: demo-devices
      description: REST surface for demo-devices.
      operations:
      - method: POST
        name: post
        description: DemoDeviceList
        call: ixon-demodevicelist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-demodevicelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — DemoDeviceList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: demodevicelist
      description: DemoDeviceList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-demodevicelist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
