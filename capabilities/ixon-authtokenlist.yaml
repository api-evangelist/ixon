naftiko: 1.0.0-alpha2
info:
  label: IXON API — AuthTokenList
  description: 'IXON API — AuthTokenList. 1 operations. Lead operation: AuthTokenList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AuthTokenList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-authtokenlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AuthTokenList business capability. Self-contained, no shared references.
    resources:
    - name: auth-tokens
      path: /auth-tokens
      operations:
      - name: get
        method: GET
        description: AuthTokenList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-authtokenlist-rest
    port: 8080
    description: REST adapter for IXON API — AuthTokenList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/auth-tokens
      name: auth-tokens
      description: REST surface for auth-tokens.
      operations:
      - method: GET
        name: get
        description: AuthTokenList
        call: ixon-authtokenlist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-authtokenlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AuthTokenList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: authtokenlist
      description: AuthTokenList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-authtokenlist.get
      outputParameters:
      - type: object
        mapping: $.
