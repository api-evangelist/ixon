naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequest
  description: 'IXON API — AgentAccessRequest. 1 operations. Lead operation: AgentAccessRequest. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequest
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequest
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequest business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-access-requests-publicId
      path: /agents/{agentId}/access-requests/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentAccessRequest
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequest-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequest. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/access-requests/{publicid}
      name: agents-agentid-access-requests-publicid
      description: REST surface for agents-agentId-access-requests-publicId.
      operations:
      - method: GET
        name: get
        description: AgentAccessRequest
        call: ixon-agentaccessrequest.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequest-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequest. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentaccessrequest
      description: AgentAccessRequest
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentaccessrequest.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
