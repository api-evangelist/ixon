naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyOtpAuthDisable
  description: 'IXON API — MyOtpAuthDisable. 2 operations. Lead operation: MyOtpAuthDisable. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MyOtpAuthDisable
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myotpauthdisable
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyOtpAuthDisable business capability. Self-contained, no shared references.
    resources:
    - name: otp-auth-disable-me
      path: /otp-auth-disable/me
      operations:
      - name: delete
        method: DELETE
        description: MyOtpAuthDisable
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: get
        method: GET
        description: MyOtpAuthDisable
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myotpauthdisable-rest
    port: 8080
    description: REST adapter for IXON API — MyOtpAuthDisable. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/otp-auth-disable/me
      name: otp-auth-disable-me
      description: REST surface for otp-auth-disable-me.
      operations:
      - method: DELETE
        name: delete
        description: MyOtpAuthDisable
        call: ixon-myotpauthdisable.delete
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: get
        description: MyOtpAuthDisable
        call: ixon-myotpauthdisable.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myotpauthdisable-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyOtpAuthDisable. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myotpauthdisable
      description: MyOtpAuthDisable
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-myotpauthdisable.delete
      outputParameters:
      - type: object
        mapping: $.
    - name: myotpauthdisable-2
      description: MyOtpAuthDisable
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-myotpauthdisable.get
      outputParameters:
      - type: object
        mapping: $.
