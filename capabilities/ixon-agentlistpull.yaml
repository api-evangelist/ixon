naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentListPull
  description: 'IXON API — AgentListPull. 1 operations. Lead operation: AgentListPull. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentListPull
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlistpull
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentListPull business capability. Self-contained, no shared references.
    resources:
    - name: agents-pull
      path: /agents/pull
      operations:
      - name: post
        method: POST
        description: AgentListPull
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlistpull-rest
    port: 8080
    description: REST adapter for IXON API — AgentListPull. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/pull
      name: agents-pull
      description: REST surface for agents-pull.
      operations:
      - method: POST
        name: post
        description: AgentListPull
        call: ixon-agentlistpull.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlistpull-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentListPull. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agentlistpull
      description: AgentListPull
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentlistpull.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
