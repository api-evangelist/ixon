naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentPageLinkList
  description: 'IXON API — AgentPageLinkList. 4 operations. Lead operation: AgentPageLinkList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentPageLinkList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentpagelinklist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentPageLinkList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-page-links
      path: /agents/{agentId}/page-links
      operations:
      - name: get
        method: GET
        description: AgentPageLinkList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
      - name: post
        method: POST
        description: AgentPageLinkList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patch
        method: PATCH
        description: AgentPageLinkList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentPageLinkList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentpagelinklist-rest
    port: 8080
    description: REST adapter for IXON API — AgentPageLinkList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/page-links
      name: agents-agentid-page-links
      description: REST surface for agents-agentId-page-links.
      operations:
      - method: GET
        name: get
        description: AgentPageLinkList
        call: ixon-agentpagelinklist.get
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: AgentPageLinkList
        call: ixon-agentpagelinklist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentPageLinkList
        call: ixon-agentpagelinklist.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentPageLinkList
        call: ixon-agentpagelinklist.delete
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentpagelinklist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentPageLinkList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentpagelinklist
      description: AgentPageLinkList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentpagelinklist.get
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentpagelinklist-2
      description: AgentPageLinkList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentpagelinklist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentpagelinklist-3
      description: AgentPageLinkList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentpagelinklist.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentpagelinklist-4
      description: AgentPageLinkList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentpagelinklist.delete
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
