naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetLibraryInstanceDetachList
  description: 'IXON API — AssetLibraryInstanceDetachList. 1 operations. Lead operation: AssetLibraryInstanceDetachList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetLibraryInstanceDetachList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetlibraryinstancedetachlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetLibraryInstanceDetachList business capability. Self-contained, no shared references.
    resources:
    - name: assets-library-instances-detach
      path: /assets/library/instances/detach
      operations:
      - name: patch
        method: PATCH
        description: AssetLibraryInstanceDetachList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetlibraryinstancedetachlist-rest
    port: 8080
    description: REST adapter for IXON API — AssetLibraryInstanceDetachList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/assets/library/instances/detach
      name: assets-library-instances-detach
      description: REST surface for assets-library-instances-detach.
      operations:
      - method: PATCH
        name: patch
        description: AssetLibraryInstanceDetachList
        call: ixon-assetlibraryinstancedetachlist.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetlibraryinstancedetachlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetLibraryInstanceDetachList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: assetlibraryinstancedetachlist
      description: AssetLibraryInstanceDetachList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-assetlibraryinstancedetachlist.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
