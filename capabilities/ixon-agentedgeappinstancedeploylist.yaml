naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceDeployList
  description: 'IXON API — AgentEdgeAppInstanceDeployList. 1 operations. Lead operation: AgentEdgeAppInstanceDeployList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceDeployList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstancedeploylist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceDeployList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances-deploy
      path: /agents/{agentId}/edge-app-instances/deploy
      operations:
      - name: post
        method: POST
        description: AgentEdgeAppInstanceDeployList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstancedeploylist-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceDeployList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances/deploy
      name: agents-agentid-edge-app-instances-deploy
      description: REST surface for agents-agentId-edge-app-instances-deploy.
      operations:
      - method: POST
        name: post
        description: AgentEdgeAppInstanceDeployList
        call: ixon-agentedgeappinstancedeploylist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstancedeploylist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceDeployList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentedgeappinstancedeploylist
      description: AgentEdgeAppInstanceDeployList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentedgeappinstancedeploylist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
