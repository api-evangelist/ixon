naftiko: 1.0.0-alpha2
info:
  label: IXON API — MqttBrokerClientMessage
  description: 'IXON API — MqttBrokerClientMessage. 1 operations. Lead operation: MqttBrokerClientMessage. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MqttBrokerClientMessage
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-mqttbrokerclientmessage
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MqttBrokerClientMessage business capability. Self-contained, no shared references.
    resources:
    - name: mqtt-brokers-brokerId-clients-publicId-message
      path: /mqtt-brokers/{brokerId}/clients/{publicId}/message
      operations:
      - name: post
        method: POST
        description: MqttBrokerClientMessage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: brokerId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-mqttbrokerclientmessage-rest
    port: 8080
    description: REST adapter for IXON API — MqttBrokerClientMessage. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/mqtt-brokers/{brokerid}/clients/{publicid}/message
      name: mqtt-brokers-brokerid-clients-publicid-message
      description: REST surface for mqtt-brokers-brokerId-clients-publicId-message.
      operations:
      - method: POST
        name: post
        description: MqttBrokerClientMessage
        call: ixon-mqttbrokerclientmessage.post
        with:
          brokerId: rest.brokerId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-mqttbrokerclientmessage-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MqttBrokerClientMessage. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: mqttbrokerclientmessage
      description: MqttBrokerClientMessage
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-mqttbrokerclientmessage.post
      with:
        brokerId: tools.brokerId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
