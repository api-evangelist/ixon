naftiko: 1.0.0-alpha2
info:
  label: IXON API — MachineGroupList
  description: 'IXON API — MachineGroupList. 1 operations. Lead operation: MachineGroupList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MachineGroupList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-machinegrouplist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MachineGroupList business capability. Self-contained, no shared references.
    resources:
    - name: agents-groups
      path: /agents/groups
      operations:
      - name: post
        method: POST
        description: MachineGroupList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-machinegrouplist-rest
    port: 8080
    description: REST adapter for IXON API — MachineGroupList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/groups
      name: agents-groups
      description: REST surface for agents-groups.
      operations:
      - method: POST
        name: post
        description: MachineGroupList
        call: ixon-machinegrouplist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-machinegrouplist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MachineGroupList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: machinegrouplist
      description: MachineGroupList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-machinegrouplist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
