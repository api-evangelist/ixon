naftiko: 1.0.0-alpha2
info:
  label: IXON API — InviteFinish
  description: 'IXON API — InviteFinish. 1 operations. Lead operation: InviteFinish. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - InviteFinish
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-invitefinish
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — InviteFinish business capability. Self-contained, no shared references.
    resources:
    - name: invites-publicId-finish
      path: /invites/{publicId}/finish
      operations:
      - name: post
        method: POST
        description: InviteFinish
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-invitefinish-rest
    port: 8080
    description: REST adapter for IXON API — InviteFinish. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/invites/{publicid}/finish
      name: invites-publicid-finish
      description: REST surface for invites-publicId-finish.
      operations:
      - method: POST
        name: post
        description: InviteFinish
        call: ixon-invitefinish.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-invitefinish-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — InviteFinish. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: invitefinish
      description: InviteFinish
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-invitefinish.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
