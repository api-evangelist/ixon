naftiko: 1.0.0-alpha2
info:
  label: IXON API — LicenceList
  description: 'IXON API — LicenceList. 2 operations. Lead operation: LicenceList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - LicenceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-licencelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — LicenceList business capability. Self-contained, no shared references.
    resources:
    - name: licences
      path: /licences
      operations:
      - name: get
        method: GET
        description: LicenceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: LicenceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-licencelist-rest
    port: 8080
    description: REST adapter for IXON API — LicenceList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/licences
      name: licences
      description: REST surface for licences.
      operations:
      - method: GET
        name: get
        description: LicenceList
        call: ixon-licencelist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: LicenceList
        call: ixon-licencelist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-licencelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — LicenceList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: licencelist
      description: LicenceList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-licencelist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: licencelist-2
      description: LicenceList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-licencelist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
