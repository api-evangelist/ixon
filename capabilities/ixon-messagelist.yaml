naftiko: 1.0.0-alpha2
info:
  label: IXON API — MessageList
  description: 'IXON API — MessageList. 3 operations. Lead operation: MessageList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - MessageList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-messagelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MessageList business capability. Self-contained, no shared references.
    resources:
    - name: messages
      path: /messages
      operations:
      - name: get
        method: GET
        description: MessageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: patch
        method: PATCH
        description: MessageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: MessageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-messagelist-rest
    port: 8080
    description: REST adapter for IXON API — MessageList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/messages
      name: messages
      description: REST surface for messages.
      operations:
      - method: GET
        name: get
        description: MessageList
        call: ixon-messagelist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: MessageList
        call: ixon-messagelist.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MessageList
        call: ixon-messagelist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-messagelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MessageList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: messagelist
      description: MessageList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-messagelist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: messagelist-2
      description: MessageList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-messagelist.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: messagelist-3
      description: MessageList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-messagelist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
