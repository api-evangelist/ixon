naftiko: 1.0.0-alpha2
info:
  label: IXON API — PrivacyStatementDownload
  description: 'IXON API — PrivacyStatementDownload. 1 operations. Lead operation: PrivacyStatementDownload. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PrivacyStatementDownload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-privacystatementdownload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PrivacyStatementDownload business capability. Self-contained, no shared references.
    resources:
    - name: privacy-statements-publicId-download
      path: /privacy-statements/{publicId}/download
      operations:
      - name: get
        method: GET
        description: PrivacyStatementDownload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-privacystatementdownload-rest
    port: 8080
    description: REST adapter for IXON API — PrivacyStatementDownload. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/privacy-statements/{publicid}/download
      name: privacy-statements-publicid-download
      description: REST surface for privacy-statements-publicId-download.
      operations:
      - method: GET
        name: get
        description: PrivacyStatementDownload
        call: ixon-privacystatementdownload.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-privacystatementdownload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PrivacyStatementDownload. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: privacystatementdownload
      description: PrivacyStatementDownload
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-privacystatementdownload.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
