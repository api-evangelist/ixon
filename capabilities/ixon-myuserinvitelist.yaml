naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserInviteList
  description: 'IXON API — MyUserInviteList. 2 operations. Lead operation: MyUserInviteList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserInviteList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserinvitelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserInviteList business capability. Self-contained, no shared references.
    resources:
    - name: users-me-invites
      path: /users/me/invites
      operations:
      - name: get
        method: GET
        description: MyUserInviteList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: delete
        method: DELETE
        description: MyUserInviteList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserinvitelist-rest
    port: 8080
    description: REST adapter for IXON API — MyUserInviteList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/me/invites
      name: users-me-invites
      description: REST surface for users-me-invites.
      operations:
      - method: GET
        name: get
        description: MyUserInviteList
        call: ixon-myuserinvitelist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MyUserInviteList
        call: ixon-myuserinvitelist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserinvitelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserInviteList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myuserinvitelist
      description: MyUserInviteList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-myuserinvitelist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: myuserinvitelist-2
      description: MyUserInviteList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-myuserinvitelist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
