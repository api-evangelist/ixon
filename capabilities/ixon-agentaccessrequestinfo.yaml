naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequestInfo
  description: 'IXON API — AgentAccessRequestInfo. 1 operations. Lead operation: AgentAccessRequestInfo. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequestInfo
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequestinfo
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequestInfo business capability. Self-contained, no shared references.
    resources:
    - name: agents-access-requests
      path: /agents/access-requests
      operations:
      - name: get
        method: GET
        description: AgentAccessRequestInfo
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequestinfo-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequestInfo. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/access-requests
      name: agents-access-requests
      description: REST surface for agents-access-requests.
      operations:
      - method: GET
        name: get
        description: AgentAccessRequestInfo
        call: ixon-agentaccessrequestinfo.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequestinfo-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequestInfo. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentaccessrequestinfo
      description: AgentAccessRequestInfo
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentaccessrequestinfo.get
      outputParameters:
      - type: object
        mapping: $.
