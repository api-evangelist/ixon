naftiko: 1.0.0-alpha2
info:
  label: IXON API — WebaccessSessionRecordingDownload
  description: 'IXON API — WebaccessSessionRecordingDownload. 1 operations. Lead operation: WebaccessSessionRecordingDownload.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - WebaccessSessionRecordingDownload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-webaccesssessionrecordingdownload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — WebaccessSessionRecordingDownload business capability. Self-contained, no shared references.
    resources:
    - name: web-access-session-recordings-publicId-download
      path: /web-access/session-recordings/{publicId}/download
      operations:
      - name: get
        method: GET
        description: WebaccessSessionRecordingDownload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-webaccesssessionrecordingdownload-rest
    port: 8080
    description: REST adapter for IXON API — WebaccessSessionRecordingDownload. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/web-access/session-recordings/{publicid}/download
      name: web-access-session-recordings-publicid-download
      description: REST surface for web-access-session-recordings-publicId-download.
      operations:
      - method: GET
        name: get
        description: WebaccessSessionRecordingDownload
        call: ixon-webaccesssessionrecordingdownload.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-webaccesssessionrecordingdownload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — WebaccessSessionRecordingDownload. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: webaccesssessionrecordingdownload
      description: WebaccessSessionRecordingDownload
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-webaccesssessionrecordingdownload.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
