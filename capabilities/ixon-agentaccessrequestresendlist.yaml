naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequestResendList
  description: 'IXON API — AgentAccessRequestResendList. 1 operations. Lead operation: AgentAccessRequestResendList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequestResendList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequestresendlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequestResendList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-access-requests-resend
      path: /agents/{agentId}/access-requests/resend
      operations:
      - name: post
        method: POST
        description: AgentAccessRequestResendList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequestresendlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequestResendList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/access-requests/resend
      name: agents-agentid-access-requests-resend
      description: REST surface for agents-agentId-access-requests-resend.
      operations:
      - method: POST
        name: post
        description: AgentAccessRequestResendList
        call: ixon-agentaccessrequestresendlist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequestresendlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequestResendList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentaccessrequestresendlist
      description: AgentAccessRequestResendList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentaccessrequestresendlist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
