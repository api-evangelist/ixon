naftiko: 1.0.0-alpha2
info:
  label: IXON API — AccessRecoverList
  description: 'IXON API — AccessRecoverList. 1 operations. Lead operation: AccessRecoverList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AccessRecoverList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-accessrecoverlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AccessRecoverList business capability. Self-contained, no shared references.
    resources:
    - name: access-recover
      path: /access-recover
      operations:
      - name: post
        method: POST
        description: AccessRecoverList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-accessrecoverlist-rest
    port: 8080
    description: REST adapter for IXON API — AccessRecoverList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/access-recover
      name: access-recover
      description: REST surface for access-recover.
      operations:
      - method: POST
        name: post
        description: AccessRecoverList
        call: ixon-accessrecoverlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-accessrecoverlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AccessRecoverList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: accessrecoverlist
      description: AccessRecoverList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-accessrecoverlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
