naftiko: 1.0.0-alpha2
info:
  label: IXON API — PageComponentTemplateVersionUpload
  description: 'IXON API — PageComponentTemplateVersionUpload. 1 operations. Lead operation: PageComponentTemplateVersionUpload.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PageComponentTemplateVersionUpload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pagecomponenttemplateversionupload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PageComponentTemplateVersionUpload business capability. Self-contained, no shared references.
    resources:
    - name: page-component-templates-publicId-version-upload
      path: /page-component-templates/{publicId}/version-upload
      operations:
      - name: post
        method: POST
        description: PageComponentTemplateVersionUpload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pagecomponenttemplateversionupload-rest
    port: 8080
    description: REST adapter for IXON API — PageComponentTemplateVersionUpload. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/page-component-templates/{publicid}/version-upload
      name: page-component-templates-publicid-version-upload
      description: REST surface for page-component-templates-publicId-version-upload.
      operations:
      - method: POST
        name: post
        description: PageComponentTemplateVersionUpload
        call: ixon-pagecomponenttemplateversionupload.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pagecomponenttemplateversionupload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PageComponentTemplateVersionUpload. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: pagecomponenttemplateversionupload
      description: PageComponentTemplateVersionUpload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-pagecomponenttemplateversionupload.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
