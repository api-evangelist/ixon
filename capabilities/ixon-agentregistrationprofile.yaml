naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRegistrationProfile
  description: 'IXON API — AgentRegistrationProfile. 3 operations. Lead operation: AgentRegistrationProfile. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRegistrationProfile
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentregistrationprofile
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRegistrationProfile business capability. Self-contained, no shared references.
    resources:
    - name: agent-registration-profiles-publicId
      path: /agent-registration-profiles/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentRegistrationProfile
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentRegistrationProfile
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentRegistrationProfile
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentregistrationprofile-rest
    port: 8080
    description: REST adapter for IXON API — AgentRegistrationProfile. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agent-registration-profiles/{publicid}
      name: agent-registration-profiles-publicid
      description: REST surface for agent-registration-profiles-publicId.
      operations:
      - method: GET
        name: get
        description: AgentRegistrationProfile
        call: ixon-agentregistrationprofile.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentRegistrationProfile
        call: ixon-agentregistrationprofile.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentRegistrationProfile
        call: ixon-agentregistrationprofile.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentregistrationprofile-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRegistrationProfile. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentregistrationprofile
      description: AgentRegistrationProfile
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentregistrationprofile.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentregistrationprofile-2
      description: AgentRegistrationProfile
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentregistrationprofile.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentregistrationprofile-3
      description: AgentRegistrationProfile
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentregistrationprofile.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
