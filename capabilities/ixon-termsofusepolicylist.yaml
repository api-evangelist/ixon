naftiko: 1.0.0-alpha2
info:
  label: IXON API — TermsOfUsePolicyList
  description: 'IXON API — TermsOfUsePolicyList. 1 operations. Lead operation: TermsOfUsePolicyList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - TermsOfUsePolicyList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-termsofusepolicylist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — TermsOfUsePolicyList business capability. Self-contained, no shared references.
    resources:
    - name: terms-of-use-policies
      path: /terms-of-use-policies
      operations:
      - name: get
        method: GET
        description: TermsOfUsePolicyList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-termsofusepolicylist-rest
    port: 8080
    description: REST adapter for IXON API — TermsOfUsePolicyList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/terms-of-use-policies
      name: terms-of-use-policies
      description: REST surface for terms-of-use-policies.
      operations:
      - method: GET
        name: get
        description: TermsOfUsePolicyList
        call: ixon-termsofusepolicylist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-termsofusepolicylist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — TermsOfUsePolicyList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: termsofusepolicylist
      description: TermsOfUsePolicyList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-termsofusepolicylist.get
      outputParameters:
      - type: object
        mapping: $.
