naftiko: 1.0.0-alpha2
info:
  label: IXON API — AuthTokenDataList
  description: 'IXON API — AuthTokenDataList. 1 operations. Lead operation: AuthTokenDataList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AuthTokenDataList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-authtokendatalist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AuthTokenDataList business capability. Self-contained, no shared references.
    resources:
    - name: auth-tokens-data
      path: /auth-tokens/data
      operations:
      - name: post
        method: POST
        description: AuthTokenDataList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-authtokendatalist-rest
    port: 8080
    description: REST adapter for IXON API — AuthTokenDataList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/auth-tokens/data
      name: auth-tokens-data
      description: REST surface for auth-tokens-data.
      operations:
      - method: POST
        name: post
        description: AuthTokenDataList
        call: ixon-authtokendatalist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-authtokendatalist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AuthTokenDataList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: authtokendatalist
      description: AuthTokenDataList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-authtokendatalist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
