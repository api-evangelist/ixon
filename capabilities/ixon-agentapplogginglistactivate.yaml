naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAppLoggingListActivate
  description: 'IXON API — AgentAppLoggingListActivate. 1 operations. Lead operation: AgentAppLoggingListActivate. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAppLoggingListActivate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentapplogginglistactivate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAppLoggingListActivate business capability. Self-contained, no shared references.
    resources:
    - name: agents-app-logging-activate
      path: /agents/app/logging/activate
      operations:
      - name: post
        method: POST
        description: AgentAppLoggingListActivate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentapplogginglistactivate-rest
    port: 8080
    description: REST adapter for IXON API — AgentAppLoggingListActivate. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/app/logging/activate
      name: agents-app-logging-activate
      description: REST surface for agents-app-logging-activate.
      operations:
      - method: POST
        name: post
        description: AgentAppLoggingListActivate
        call: ixon-agentapplogginglistactivate.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentapplogginglistactivate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAppLoggingListActivate. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentapplogginglistactivate
      description: AgentAppLoggingListActivate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentapplogginglistactivate.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
