naftiko: 1.0.0-alpha2
info:
  label: IXON API — PageDraftList
  description: 'IXON API — PageDraftList. 1 operations. Lead operation: PageDraftList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - PageDraftList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pagedraftlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PageDraftList business capability. Self-contained, no shared references.
    resources:
    - name: pages-draft
      path: /pages/draft
      operations:
      - name: post
        method: POST
        description: PageDraftList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pagedraftlist-rest
    port: 8080
    description: REST adapter for IXON API — PageDraftList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/pages/draft
      name: pages-draft
      description: REST surface for pages-draft.
      operations:
      - method: POST
        name: post
        description: PageDraftList
        call: ixon-pagedraftlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pagedraftlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PageDraftList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: pagedraftlist
      description: PageDraftList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-pagedraftlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
