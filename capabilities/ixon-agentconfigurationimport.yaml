naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentConfigurationImport
  description: 'IXON API — AgentConfigurationImport. 1 operations. Lead operation: AgentConfigurationImport. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentConfigurationImport
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentconfigurationimport
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentConfigurationImport business capability. Self-contained, no shared references.
    resources:
    - name: agents-publicId-configuration-import
      path: /agents/{publicId}/configuration/import
      operations:
      - name: put
        method: PUT
        description: AgentConfigurationImport
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentconfigurationimport-rest
    port: 8080
    description: REST adapter for IXON API — AgentConfigurationImport. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{publicid}/configuration/import
      name: agents-publicid-configuration-import
      description: REST surface for agents-publicId-configuration-import.
      operations:
      - method: PUT
        name: put
        description: AgentConfigurationImport
        call: ixon-agentconfigurationimport.put
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentconfigurationimport-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentConfigurationImport. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentconfigurationimport
      description: AgentConfigurationImport
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentconfigurationimport.put
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
