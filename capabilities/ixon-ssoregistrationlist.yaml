naftiko: 1.0.0-alpha2
info:
  label: IXON API — SsoRegistrationList
  description: 'IXON API — SsoRegistrationList. 1 operations. Lead operation: SsoRegistrationList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - SsoRegistrationList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-ssoregistrationlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — SsoRegistrationList business capability. Self-contained, no shared references.
    resources:
    - name: sso-registrations
      path: /sso/registrations
      operations:
      - name: post
        method: POST
        description: SsoRegistrationList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-ssoregistrationlist-rest
    port: 8080
    description: REST adapter for IXON API — SsoRegistrationList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/sso/registrations
      name: sso-registrations
      description: REST surface for sso-registrations.
      operations:
      - method: POST
        name: post
        description: SsoRegistrationList
        call: ixon-ssoregistrationlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-ssoregistrationlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — SsoRegistrationList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: ssoregistrationlist
      description: SsoRegistrationList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-ssoregistrationlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
