naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetLibraryVersionList
  description: 'IXON API — AssetLibraryVersionList. 1 operations. Lead operation: AssetLibraryVersionList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetLibraryVersionList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetlibraryversionlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetLibraryVersionList business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetId-library-versions
      path: /assets/{assetId}/library/versions
      operations:
      - name: get
        method: GET
        description: AssetLibraryVersionList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assetId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetlibraryversionlist-rest
    port: 8080
    description: REST adapter for IXON API — AssetLibraryVersionList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/assets/{assetid}/library/versions
      name: assets-assetid-library-versions
      description: REST surface for assets-assetId-library-versions.
      operations:
      - method: GET
        name: get
        description: AssetLibraryVersionList
        call: ixon-assetlibraryversionlist.get
        with:
          assetId: rest.assetId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetlibraryversionlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetLibraryVersionList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: assetlibraryversionlist
      description: AssetLibraryVersionList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-assetlibraryversionlist.get
      with:
        assetId: tools.assetId
      outputParameters:
      - type: object
        mapping: $.
