naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserPushDeviceReleaseClaim
  description: 'IXON API — MyUserPushDeviceReleaseClaim. 1 operations. Lead operation: MyUserPushDeviceReleaseClaim. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserPushDeviceReleaseClaim
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserpushdevicereleaseclaim
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserPushDeviceReleaseClaim business capability. Self-contained, no shared references.
    resources:
    - name: users-me-push-devices-release-claim
      path: /users/me/push-devices/release-claim
      operations:
      - name: post
        method: POST
        description: MyUserPushDeviceReleaseClaim
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserpushdevicereleaseclaim-rest
    port: 8080
    description: REST adapter for IXON API — MyUserPushDeviceReleaseClaim. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/push-devices/release-claim
      name: users-me-push-devices-release-claim
      description: REST surface for users-me-push-devices-release-claim.
      operations:
      - method: POST
        name: post
        description: MyUserPushDeviceReleaseClaim
        call: ixon-myuserpushdevicereleaseclaim.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserpushdevicereleaseclaim-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserPushDeviceReleaseClaim. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: myuserpushdevicereleaseclaim
      description: MyUserPushDeviceReleaseClaim
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserpushdevicereleaseclaim.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
