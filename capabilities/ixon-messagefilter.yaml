naftiko: 1.0.0-alpha2
info:
  label: IXON API — MessageFilter
  description: 'IXON API — MessageFilter. 3 operations. Lead operation: MessageFilter. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - MessageFilter
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-messagefilter
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MessageFilter business capability. Self-contained, no shared references.
    resources:
    - name: message-filters-publicId
      path: /message-filters/{publicId}
      operations:
      - name: get
        method: GET
        description: MessageFilter
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: MessageFilter
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: MessageFilter
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-messagefilter-rest
    port: 8080
    description: REST adapter for IXON API — MessageFilter. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/message-filters/{publicid}
      name: message-filters-publicid
      description: REST surface for message-filters-publicId.
      operations:
      - method: GET
        name: get
        description: MessageFilter
        call: ixon-messagefilter.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: MessageFilter
        call: ixon-messagefilter.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MessageFilter
        call: ixon-messagefilter.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-messagefilter-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MessageFilter. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: messagefilter
      description: MessageFilter
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-messagefilter.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: messagefilter-2
      description: MessageFilter
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-messagefilter.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: messagefilter-3
      description: MessageFilter
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-messagefilter.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
