naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetObjectStorageDeleteAction
  description: 'IXON API — AssetObjectStorageDeleteAction. 1 operations. Lead operation: AssetObjectStorageDeleteAction. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetObjectStorageDeleteAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetobjectstoragedeleteaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetObjectStorageDeleteAction business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetId-object-storage-delete
      path: /assets/{assetId}/object-storage/delete
      operations:
      - name: post
        method: POST
        description: AssetObjectStorageDeleteAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assetId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetobjectstoragedeleteaction-rest
    port: 8080
    description: REST adapter for IXON API — AssetObjectStorageDeleteAction. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/assets/{assetid}/object-storage/delete
      name: assets-assetid-object-storage-delete
      description: REST surface for assets-assetId-object-storage-delete.
      operations:
      - method: POST
        name: post
        description: AssetObjectStorageDeleteAction
        call: ixon-assetobjectstoragedeleteaction.post
        with:
          assetId: rest.assetId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetobjectstoragedeleteaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetObjectStorageDeleteAction. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: assetobjectstoragedeleteaction
      description: AssetObjectStorageDeleteAction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-assetobjectstoragedeleteaction.post
      with:
        assetId: tools.assetId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
