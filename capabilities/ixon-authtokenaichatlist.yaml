naftiko: 1.0.0-alpha2
info:
  label: IXON API — AuthTokenAiChatList
  description: 'IXON API — AuthTokenAiChatList. 1 operations. Lead operation: AuthTokenAiChatList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AuthTokenAiChatList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-authtokenaichatlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AuthTokenAiChatList business capability. Self-contained, no shared references.
    resources:
    - name: auth-tokens-ai-chat
      path: /auth-tokens/ai-chat
      operations:
      - name: post
        method: POST
        description: AuthTokenAiChatList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-authtokenaichatlist-rest
    port: 8080
    description: REST adapter for IXON API — AuthTokenAiChatList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/auth-tokens/ai-chat
      name: auth-tokens-ai-chat
      description: REST surface for auth-tokens-ai-chat.
      operations:
      - method: POST
        name: post
        description: AuthTokenAiChatList
        call: ixon-authtokenaichatlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-authtokenaichatlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AuthTokenAiChatList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: authtokenaichatlist
      description: AuthTokenAiChatList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-authtokenaichatlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
