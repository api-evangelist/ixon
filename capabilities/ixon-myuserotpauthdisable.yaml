naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserOtpAuthDisable
  description: 'IXON API — MyUserOtpAuthDisable. 1 operations. Lead operation: MyUserOtpAuthDisable. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserOtpAuthDisable
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserotpauthdisable
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserOtpAuthDisable business capability. Self-contained, no shared references.
    resources:
    - name: users-me-otp-auth-disable
      path: /users/me/otp-auth/disable
      operations:
      - name: post
        method: POST
        description: MyUserOtpAuthDisable
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserotpauthdisable-rest
    port: 8080
    description: REST adapter for IXON API — MyUserOtpAuthDisable. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/otp-auth/disable
      name: users-me-otp-auth-disable
      description: REST surface for users-me-otp-auth-disable.
      operations:
      - method: POST
        name: post
        description: MyUserOtpAuthDisable
        call: ixon-myuserotpauthdisable.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserotpauthdisable-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserOtpAuthDisable. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myuserotpauthdisable
      description: MyUserOtpAuthDisable
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserotpauthdisable.post
      outputParameters:
      - type: object
        mapping: $.
