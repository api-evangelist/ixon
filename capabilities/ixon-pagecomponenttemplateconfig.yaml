naftiko: 1.0.0-alpha2
info:
  label: IXON API — PageComponentTemplateConfig
  description: 'IXON API — PageComponentTemplateConfig. 1 operations. Lead operation: PageComponentTemplateConfig. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PageComponentTemplateConfig
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pagecomponenttemplateconfig
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PageComponentTemplateConfig business capability. Self-contained, no shared references.
    resources:
    - name: page-component-template-configs-publicId
      path: /page-component-template-configs/{publicId}
      operations:
      - name: get
        method: GET
        description: PageComponentTemplateConfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pagecomponenttemplateconfig-rest
    port: 8080
    description: REST adapter for IXON API — PageComponentTemplateConfig. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/page-component-template-configs/{publicid}
      name: page-component-template-configs-publicid
      description: REST surface for page-component-template-configs-publicId.
      operations:
      - method: GET
        name: get
        description: PageComponentTemplateConfig
        call: ixon-pagecomponenttemplateconfig.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pagecomponenttemplateconfig-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PageComponentTemplateConfig. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pagecomponenttemplateconfig
      description: PageComponentTemplateConfig
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-pagecomponenttemplateconfig.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
