naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentTemplateVersion
  description: 'IXON API — BackendComponentTemplateVersion. 1 operations. Lead operation: BackendComponentTemplateVersion.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentTemplateVersion
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponenttemplateversion
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentTemplateVersion business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-template-versions-publicId
      path: /backend-component-template-versions/{publicId}
      operations:
      - name: get
        method: GET
        description: BackendComponentTemplateVersion
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponenttemplateversion-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentTemplateVersion. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/backend-component-template-versions/{publicid}
      name: backend-component-template-versions-publicid
      description: REST surface for backend-component-template-versions-publicId.
      operations:
      - method: GET
        name: get
        description: BackendComponentTemplateVersion
        call: ixon-backendcomponenttemplateversion.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponenttemplateversion-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentTemplateVersion. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: backendcomponenttemplateversion
      description: BackendComponentTemplateVersion
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-backendcomponenttemplateversion.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
