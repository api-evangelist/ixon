naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetPageList
  description: 'IXON API — AssetPageList. 1 operations. Lead operation: AssetPageList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AssetPageList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetpagelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetPageList business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetId-pages
      path: /assets/{assetId}/pages
      operations:
      - name: get
        method: GET
        description: AssetPageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assetId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetpagelist-rest
    port: 8080
    description: REST adapter for IXON API — AssetPageList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/assets/{assetid}/pages
      name: assets-assetid-pages
      description: REST surface for assets-assetId-pages.
      operations:
      - method: GET
        name: get
        description: AssetPageList
        call: ixon-assetpagelist.get
        with:
          assetId: rest.assetId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetpagelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetPageList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: assetpagelist
      description: AssetPageList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-assetpagelist.get
      with:
        assetId: tools.assetId
      outputParameters:
      - type: object
        mapping: $.
