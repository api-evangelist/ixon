naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequestApprove
  description: 'IXON API — AgentAccessRequestApprove. 1 operations. Lead operation: AgentAccessRequestApprove. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequestApprove
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequestapprove
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequestApprove business capability. Self-contained, no shared references.
    resources:
    - name: agents-access-requests-approve
      path: /agents/access-requests/approve
      operations:
      - name: post
        method: POST
        description: AgentAccessRequestApprove
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequestapprove-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequestApprove. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/access-requests/approve
      name: agents-access-requests-approve
      description: REST surface for agents-access-requests-approve.
      operations:
      - method: POST
        name: post
        description: AgentAccessRequestApprove
        call: ixon-agentaccessrequestapprove.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequestapprove-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequestApprove. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentaccessrequestapprove
      description: AgentAccessRequestApprove
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentaccessrequestapprove.post
      outputParameters:
      - type: object
        mapping: $.
