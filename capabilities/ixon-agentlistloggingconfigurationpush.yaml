naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentListLoggingConfigurationPush
  description: 'IXON API — AgentListLoggingConfigurationPush. 1 operations. Lead operation: AgentListLoggingConfigurationPush.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentListLoggingConfigurationPush
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlistloggingconfigurationpush
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentListLoggingConfigurationPush business capability. Self-contained, no shared references.
    resources:
    - name: agents-logging-configuration-push
      path: /agents/logging-configuration/push
      operations:
      - name: post
        method: POST
        description: AgentListLoggingConfigurationPush
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlistloggingconfigurationpush-rest
    port: 8080
    description: REST adapter for IXON API — AgentListLoggingConfigurationPush. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/logging-configuration/push
      name: agents-logging-configuration-push
      description: REST surface for agents-logging-configuration-push.
      operations:
      - method: POST
        name: post
        description: AgentListLoggingConfigurationPush
        call: ixon-agentlistloggingconfigurationpush.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlistloggingconfigurationpush-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentListLoggingConfigurationPush. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentlistloggingconfigurationpush
      description: AgentListLoggingConfigurationPush
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentlistloggingconfigurationpush.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
