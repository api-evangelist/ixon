naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentDataTag
  description: 'IXON API — AgentDataTag. 3 operations. Lead operation: AgentDataTag. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentDataTag
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentdatatag
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentDataTag business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-data-tags-publicId
      path: /agents/{agentId}/data-tags/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentDataTag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentDataTag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentDataTag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentdatatag-rest
    port: 8080
    description: REST adapter for IXON API — AgentDataTag. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/data-tags/{publicid}
      name: agents-agentid-data-tags-publicid
      description: REST surface for agents-agentId-data-tags-publicId.
      operations:
      - method: GET
        name: get
        description: AgentDataTag
        call: ixon-agentdatatag.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentDataTag
        call: ixon-agentdatatag.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentDataTag
        call: ixon-agentdatatag.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentdatatag-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentDataTag. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agentdatatag
      description: AgentDataTag
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentdatatag.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdatatag-2
      description: AgentDataTag
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentdatatag.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdatatag-3
      description: AgentDataTag
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentdatatag.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
