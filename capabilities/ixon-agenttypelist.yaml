naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentTypeList
  description: 'IXON API — AgentTypeList. 1 operations. Lead operation: AgentTypeList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentTypeList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agenttypelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentTypeList business capability. Self-contained, no shared references.
    resources:
    - name: agent-types
      path: /agent-types
      operations:
      - name: get
        method: GET
        description: AgentTypeList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agenttypelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentTypeList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agent-types
      name: agent-types
      description: REST surface for agent-types.
      operations:
      - method: GET
        name: get
        description: AgentTypeList
        call: ixon-agenttypelist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agenttypelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentTypeList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agenttypelist
      description: AgentTypeList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agenttypelist.get
      outputParameters:
      - type: object
        mapping: $.
