naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentObjectStorageDownloadAction
  description: 'IXON API — AgentObjectStorageDownloadAction. 1 operations. Lead operation: AgentObjectStorageDownloadAction.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentObjectStorageDownloadAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentobjectstoragedownloadaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentObjectStorageDownloadAction business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-object-storage-download
      path: /agents/{agentId}/object-storage/download
      operations:
      - name: post
        method: POST
        description: AgentObjectStorageDownloadAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentobjectstoragedownloadaction-rest
    port: 8080
    description: REST adapter for IXON API — AgentObjectStorageDownloadAction. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/object-storage/download
      name: agents-agentid-object-storage-download
      description: REST surface for agents-agentId-object-storage-download.
      operations:
      - method: POST
        name: post
        description: AgentObjectStorageDownloadAction
        call: ixon-agentobjectstoragedownloadaction.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentobjectstoragedownloadaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentObjectStorageDownloadAction. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentobjectstoragedownloadaction
      description: AgentObjectStorageDownloadAction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentobjectstoragedownloadaction.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
