naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentTemplateDownloadLogs
  description: 'IXON API — BackendComponentTemplateDownloadLogs. 1 operations. Lead operation: BackendComponentTemplateDownloadLogs.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentTemplateDownloadLogs
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponenttemplatedownloadlogs
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentTemplateDownloadLogs business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-templates-publicId-logs
      path: /backend-component-templates/{publicId}/logs
      operations:
      - name: get
        method: GET
        description: BackendComponentTemplateDownloadLogs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponenttemplatedownloadlogs-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentTemplateDownloadLogs. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/backend-component-templates/{publicid}/logs
      name: backend-component-templates-publicid-logs
      description: REST surface for backend-component-templates-publicId-logs.
      operations:
      - method: GET
        name: get
        description: BackendComponentTemplateDownloadLogs
        call: ixon-backendcomponenttemplatedownloadlogs.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponenttemplatedownloadlogs-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentTemplateDownloadLogs. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: backendcomponenttemplatedownloadlogs
      description: BackendComponentTemplateDownloadLogs
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-backendcomponenttemplatedownloadlogs.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
