naftiko: 1.0.0-alpha2
info:
  label: IXON API — Audience
  description: 'IXON API — Audience. 3 operations. Lead operation: Audience. Self-contained Naftiko capability covering one
    Ixon business surface.'
  tags:
  - Ixon
  - Audience
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-audience
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — Audience business capability. Self-contained, no shared references.
    resources:
    - name: audiences-publicId
      path: /audiences/{publicId}
      operations:
      - name: get
        method: GET
        description: Audience
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: Audience
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: Audience
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-audience-rest
    port: 8080
    description: REST adapter for IXON API — Audience. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/audiences/{publicid}
      name: audiences-publicid
      description: REST surface for audiences-publicId.
      operations:
      - method: GET
        name: get
        description: Audience
        call: ixon-audience.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: Audience
        call: ixon-audience.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: Audience
        call: ixon-audience.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-audience-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — Audience. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: audience
      description: Audience
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-audience.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: audience-2
      description: Audience
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-audience.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: audience-3
      description: Audience
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-audience.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
