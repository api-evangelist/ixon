naftiko: 1.0.0-alpha2
info:
  label: IXON API — PrivacyStatementList
  description: 'IXON API — PrivacyStatementList. 1 operations. Lead operation: PrivacyStatementList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PrivacyStatementList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-privacystatementlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PrivacyStatementList business capability. Self-contained, no shared references.
    resources:
    - name: privacy-statements
      path: /privacy-statements
      operations:
      - name: get
        method: GET
        description: PrivacyStatementList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-privacystatementlist-rest
    port: 8080
    description: REST adapter for IXON API — PrivacyStatementList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/privacy-statements
      name: privacy-statements
      description: REST surface for privacy-statements.
      operations:
      - method: GET
        name: get
        description: PrivacyStatementList
        call: ixon-privacystatementlist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-privacystatementlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PrivacyStatementList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: privacystatementlist
      description: PrivacyStatementList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-privacystatementlist.get
      outputParameters:
      - type: object
        mapping: $.
