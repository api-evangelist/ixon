naftiko: 1.0.0-alpha2
info:
  label: IXON API — EdgeAppVersionUpload
  description: 'IXON API — EdgeAppVersionUpload. 1 operations. Lead operation: EdgeAppVersionUpload. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - EdgeAppVersionUpload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-edgeappversionupload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — EdgeAppVersionUpload business capability. Self-contained, no shared references.
    resources:
    - name: edge-apps-publicId-version-upload
      path: /edge-apps/{publicId}/version-upload
      operations:
      - name: post
        method: POST
        description: EdgeAppVersionUpload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-edgeappversionupload-rest
    port: 8080
    description: REST adapter for IXON API — EdgeAppVersionUpload. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/edge-apps/{publicid}/version-upload
      name: edge-apps-publicid-version-upload
      description: REST surface for edge-apps-publicId-version-upload.
      operations:
      - method: POST
        name: post
        description: EdgeAppVersionUpload
        call: ixon-edgeappversionupload.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-edgeappversionupload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — EdgeAppVersionUpload. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: edgeappversionupload
      description: EdgeAppVersionUpload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-edgeappversionupload.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
