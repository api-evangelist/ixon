naftiko: 1.0.0-alpha2
info:
  label: IXON API — AudienceAgentReferenceList
  description: 'IXON API — AudienceAgentReferenceList. 1 operations. Lead operation: AudienceAgentReferenceList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AudienceAgentReferenceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-audienceagentreferencelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AudienceAgentReferenceList business capability. Self-contained, no shared references.
    resources:
    - name: audiences-agent
      path: /audiences/agent
      operations:
      - name: get
        method: GET
        description: AudienceAgentReferenceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-audienceagentreferencelist-rest
    port: 8080
    description: REST adapter for IXON API — AudienceAgentReferenceList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/audiences/agent
      name: audiences-agent
      description: REST surface for audiences-agent.
      operations:
      - method: GET
        name: get
        description: AudienceAgentReferenceList
        call: ixon-audienceagentreferencelist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-audienceagentreferencelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AudienceAgentReferenceList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: audienceagentreferencelist
      description: AudienceAgentReferenceList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-audienceagentreferencelist.get
      outputParameters:
      - type: object
        mapping: $.
