naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentListConfigurationPush
  description: 'IXON API — AgentListConfigurationPush. 1 operations. Lead operation: AgentListConfigurationPush. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentListConfigurationPush
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlistconfigurationpush
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentListConfigurationPush business capability. Self-contained, no shared references.
    resources:
    - name: agents-configuration-push
      path: /agents/configuration/push
      operations:
      - name: post
        method: POST
        description: AgentListConfigurationPush
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlistconfigurationpush-rest
    port: 8080
    description: REST adapter for IXON API — AgentListConfigurationPush. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/configuration/push
      name: agents-configuration-push
      description: REST surface for agents-configuration-push.
      operations:
      - method: POST
        name: post
        description: AgentListConfigurationPush
        call: ixon-agentlistconfigurationpush.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlistconfigurationpush-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentListConfigurationPush. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentlistconfigurationpush
      description: AgentListConfigurationPush
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentlistconfigurationpush.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
