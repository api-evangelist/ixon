naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentLicenceBundleActivate
  description: 'IXON API — AgentLicenceBundleActivate. 1 operations. Lead operation: AgentLicenceBundleActivate. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentLicenceBundleActivate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlicencebundleactivate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentLicenceBundleActivate business capability. Self-contained, no shared references.
    resources:
    - name: agents-licence-bundles-activate
      path: /agents/licence-bundles/activate
      operations:
      - name: post
        method: POST
        description: AgentLicenceBundleActivate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlicencebundleactivate-rest
    port: 8080
    description: REST adapter for IXON API — AgentLicenceBundleActivate. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/licence-bundles/activate
      name: agents-licence-bundles-activate
      description: REST surface for agents-licence-bundles-activate.
      operations:
      - method: POST
        name: post
        description: AgentLicenceBundleActivate
        call: ixon-agentlicencebundleactivate.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlicencebundleactivate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentLicenceBundleActivate. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentlicencebundleactivate
      description: AgentLicenceBundleActivate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentlicencebundleactivate.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
