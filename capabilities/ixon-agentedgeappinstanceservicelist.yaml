naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceServiceList
  description: 'IXON API — AgentEdgeAppInstanceServiceList. 1 operations. Lead operation: AgentEdgeAppInstanceServiceList.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceServiceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstanceservicelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceServiceList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances-services
      path: /agents/{agentId}/edge-app-instances/services
      operations:
      - name: patch
        method: PATCH
        description: AgentEdgeAppInstanceServiceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstanceservicelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceServiceList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances/services
      name: agents-agentid-edge-app-instances-services
      description: REST surface for agents-agentId-edge-app-instances-services.
      operations:
      - method: PATCH
        name: patch
        description: AgentEdgeAppInstanceServiceList
        call: ixon-agentedgeappinstanceservicelist.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstanceservicelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceServiceList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentedgeappinstanceservicelist
      description: AgentEdgeAppInstanceServiceList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentedgeappinstanceservicelist.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
