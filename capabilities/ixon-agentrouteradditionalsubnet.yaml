naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterAdditionalSubnet
  description: 'IXON API — AgentRouterAdditionalSubnet. 3 operations. Lead operation: AgentRouterAdditionalSubnet. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterAdditionalSubnet
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentrouteradditionalsubnet
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterAdditionalSubnet business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-additional-subnets-publicId
      path: /agents/{agentId}/router-additional-subnets/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentRouterAdditionalSubnet
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentRouterAdditionalSubnet
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentRouterAdditionalSubnet
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentrouteradditionalsubnet-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterAdditionalSubnet. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-additional-subnets/{publicid}
      name: agents-agentid-router-additional-subnets-publicid
      description: REST surface for agents-agentId-router-additional-subnets-publicId.
      operations:
      - method: GET
        name: get
        description: AgentRouterAdditionalSubnet
        call: ixon-agentrouteradditionalsubnet.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentRouterAdditionalSubnet
        call: ixon-agentrouteradditionalsubnet.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentRouterAdditionalSubnet
        call: ixon-agentrouteradditionalsubnet.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentrouteradditionalsubnet-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterAdditionalSubnet. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentrouteradditionalsubnet
      description: AgentRouterAdditionalSubnet
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentrouteradditionalsubnet.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouteradditionalsubnet-2
      description: AgentRouterAdditionalSubnet
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentrouteradditionalsubnet.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouteradditionalsubnet-3
      description: AgentRouterAdditionalSubnet
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentrouteradditionalsubnet.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
