naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserPushDeviceDeactivate
  description: 'IXON API — MyUserPushDeviceDeactivate. 1 operations. Lead operation: MyUserPushDeviceDeactivate. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserPushDeviceDeactivate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserpushdevicedeactivate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserPushDeviceDeactivate business capability. Self-contained, no shared references.
    resources:
    - name: users-me-push-devices-deactivate
      path: /users/me/push-devices/deactivate
      operations:
      - name: post
        method: POST
        description: MyUserPushDeviceDeactivate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserpushdevicedeactivate-rest
    port: 8080
    description: REST adapter for IXON API — MyUserPushDeviceDeactivate. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/push-devices/deactivate
      name: users-me-push-devices-deactivate
      description: REST surface for users-me-push-devices-deactivate.
      operations:
      - method: POST
        name: post
        description: MyUserPushDeviceDeactivate
        call: ixon-myuserpushdevicedeactivate.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserpushdevicedeactivate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserPushDeviceDeactivate. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: myuserpushdevicedeactivate
      description: MyUserPushDeviceDeactivate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserpushdevicedeactivate.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
