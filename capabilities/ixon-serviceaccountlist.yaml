naftiko: 1.0.0-alpha2
info:
  label: IXON API — ServiceAccountList
  description: 'IXON API — ServiceAccountList. 2 operations. Lead operation: ServiceAccountList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - ServiceAccountList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-serviceaccountlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — ServiceAccountList business capability. Self-contained, no shared references.
    resources:
    - name: users-service-account
      path: /users/service-account
      operations:
      - name: post
        method: POST
        description: ServiceAccountList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: ServiceAccountList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-serviceaccountlist-rest
    port: 8080
    description: REST adapter for IXON API — ServiceAccountList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/service-account
      name: users-service-account
      description: REST surface for users-service-account.
      operations:
      - method: POST
        name: post
        description: ServiceAccountList
        call: ixon-serviceaccountlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: ServiceAccountList
        call: ixon-serviceaccountlist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-serviceaccountlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — ServiceAccountList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: serviceaccountlist
      description: ServiceAccountList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-serviceaccountlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: serviceaccountlist-2
      description: ServiceAccountList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-serviceaccountlist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
