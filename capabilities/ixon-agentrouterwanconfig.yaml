naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterWanConfig
  description: 'IXON API — AgentRouterWanConfig. 1 operations. Lead operation: AgentRouterWanConfig. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterWanConfig
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentrouterwanconfig
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterWanConfig business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-wan-config
      path: /agents/{agentId}/router-wan-config
      operations:
      - name: patch
        method: PATCH
        description: AgentRouterWanConfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentrouterwanconfig-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterWanConfig. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-wan-config
      name: agents-agentid-router-wan-config
      description: REST surface for agents-agentId-router-wan-config.
      operations:
      - method: PATCH
        name: patch
        description: AgentRouterWanConfig
        call: ixon-agentrouterwanconfig.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentrouterwanconfig-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterWanConfig. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentrouterwanconfig
      description: AgentRouterWanConfig
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentrouterwanconfig.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
