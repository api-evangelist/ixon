naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterPortForward
  description: 'IXON API — AgentRouterPortForward. 3 operations. Lead operation: AgentRouterPortForward. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterPortForward
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentrouterportforward
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterPortForward business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-port-forwards-publicId
      path: /agents/{agentId}/router-port-forwards/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentRouterPortForward
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentRouterPortForward
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentRouterPortForward
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentrouterportforward-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterPortForward. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-port-forwards/{publicid}
      name: agents-agentid-router-port-forwards-publicid
      description: REST surface for agents-agentId-router-port-forwards-publicId.
      operations:
      - method: GET
        name: get
        description: AgentRouterPortForward
        call: ixon-agentrouterportforward.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentRouterPortForward
        call: ixon-agentrouterportforward.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentRouterPortForward
        call: ixon-agentrouterportforward.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentrouterportforward-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterPortForward. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentrouterportforward
      description: AgentRouterPortForward
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentrouterportforward.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouterportforward-2
      description: AgentRouterPortForward
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentrouterportforward.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouterportforward-3
      description: AgentRouterPortForward
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentrouterportforward.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
