naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetObjectStorageUploadAction
  description: 'IXON API — AssetObjectStorageUploadAction. 1 operations. Lead operation: AssetObjectStorageUploadAction. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetObjectStorageUploadAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetobjectstorageuploadaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetObjectStorageUploadAction business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetId-object-storage-upload
      path: /assets/{assetId}/object-storage/upload
      operations:
      - name: post
        method: POST
        description: AssetObjectStorageUploadAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assetId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetobjectstorageuploadaction-rest
    port: 8080
    description: REST adapter for IXON API — AssetObjectStorageUploadAction. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/assets/{assetid}/object-storage/upload
      name: assets-assetid-object-storage-upload
      description: REST surface for assets-assetId-object-storage-upload.
      operations:
      - method: POST
        name: post
        description: AssetObjectStorageUploadAction
        call: ixon-assetobjectstorageuploadaction.post
        with:
          assetId: rest.assetId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetobjectstorageuploadaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetObjectStorageUploadAction. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: assetobjectstorageuploadaction
      description: AssetObjectStorageUploadAction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-assetobjectstorageuploadaction.post
      with:
        assetId: tools.assetId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
