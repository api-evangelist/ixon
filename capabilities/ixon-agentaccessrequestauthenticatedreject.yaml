naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequestAuthenticatedReject
  description: 'IXON API — AgentAccessRequestAuthenticatedReject. 1 operations. Lead operation: AgentAccessRequestAuthenticatedReject.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequestAuthenticatedReject
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequestauthenticatedreject
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequestAuthenticatedReject business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-access-requests-reject-publicId
      path: /agents/{agentId}/access-requests/reject/{publicId}
      operations:
      - name: post
        method: POST
        description: AgentAccessRequestAuthenticatedReject
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequestauthenticatedreject-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequestAuthenticatedReject. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/access-requests/reject/{publicid}
      name: agents-agentid-access-requests-reject-publicid
      description: REST surface for agents-agentId-access-requests-reject-publicId.
      operations:
      - method: POST
        name: post
        description: AgentAccessRequestAuthenticatedReject
        call: ixon-agentaccessrequestauthenticatedreject.post
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequestauthenticatedreject-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequestAuthenticatedReject. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: agentaccessrequestauthenticatedreject
      description: AgentAccessRequestAuthenticatedReject
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentaccessrequestauthenticatedreject.post
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
