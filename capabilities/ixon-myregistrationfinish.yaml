naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyRegistrationFinish
  description: 'IXON API — MyRegistrationFinish. 1 operations. Lead operation: MyRegistrationFinish. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyRegistrationFinish
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myregistrationfinish
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyRegistrationFinish business capability. Self-contained, no shared references.
    resources:
    - name: registrations-me-finish
      path: /registrations/me/finish
      operations:
      - name: post
        method: POST
        description: MyRegistrationFinish
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myregistrationfinish-rest
    port: 8080
    description: REST adapter for IXON API — MyRegistrationFinish. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/registrations/me/finish
      name: registrations-me-finish
      description: REST surface for registrations-me-finish.
      operations:
      - method: POST
        name: post
        description: MyRegistrationFinish
        call: ixon-myregistrationfinish.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myregistrationfinish-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyRegistrationFinish. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myregistrationfinish
      description: MyRegistrationFinish
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myregistrationfinish.post
      outputParameters:
      - type: object
        mapping: $.
