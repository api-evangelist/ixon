naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentPageList
  description: 'IXON API — AgentPageList. 1 operations. Lead operation: AgentPageList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentPageList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentpagelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentPageList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-pages
      path: /agents/{agentId}/pages
      operations:
      - name: get
        method: GET
        description: AgentPageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentpagelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentPageList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/pages
      name: agents-agentid-pages
      description: REST surface for agents-agentId-pages.
      operations:
      - method: GET
        name: get
        description: AgentPageList
        call: ixon-agentpagelist.get
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentpagelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentPageList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agentpagelist
      description: AgentPageList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentpagelist.get
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
