naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentListConfigurationImport
  description: 'IXON API — AgentListConfigurationImport. 1 operations. Lead operation: AgentListConfigurationImport. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentListConfigurationImport
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlistconfigurationimport
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentListConfigurationImport business capability. Self-contained, no shared references.
    resources:
    - name: agents-configuration-import
      path: /agents/configuration/import
      operations:
      - name: put
        method: PUT
        description: AgentListConfigurationImport
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlistconfigurationimport-rest
    port: 8080
    description: REST adapter for IXON API — AgentListConfigurationImport. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/configuration/import
      name: agents-configuration-import
      description: REST surface for agents-configuration-import.
      operations:
      - method: PUT
        name: put
        description: AgentListConfigurationImport
        call: ixon-agentlistconfigurationimport.put
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlistconfigurationimport-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentListConfigurationImport. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentlistconfigurationimport
      description: AgentListConfigurationImport
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentlistconfigurationimport.put
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
