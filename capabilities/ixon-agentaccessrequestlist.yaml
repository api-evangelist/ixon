naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequestList
  description: 'IXON API — AgentAccessRequestList. 2 operations. Lead operation: AgentAccessRequestList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequestList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequestlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequestList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-access-requests
      path: /agents/{agentId}/access-requests
      operations:
      - name: get
        method: GET
        description: AgentAccessRequestList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
      - name: post
        method: POST
        description: AgentAccessRequestList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequestlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequestList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/access-requests
      name: agents-agentid-access-requests
      description: REST surface for agents-agentId-access-requests.
      operations:
      - method: GET
        name: get
        description: AgentAccessRequestList
        call: ixon-agentaccessrequestlist.get
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: AgentAccessRequestList
        call: ixon-agentaccessrequestlist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequestlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequestList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentaccessrequestlist
      description: AgentAccessRequestList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentaccessrequestlist.get
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentaccessrequestlist-2
      description: AgentAccessRequestList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentaccessrequestlist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
