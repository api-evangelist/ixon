naftiko: 1.0.0-alpha2
info:
  label: IXON API — PageComponentTemplateVersion
  description: 'IXON API — PageComponentTemplateVersion. 2 operations. Lead operation: PageComponentTemplateVersion. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PageComponentTemplateVersion
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pagecomponenttemplateversion
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PageComponentTemplateVersion business capability. Self-contained, no shared references.
    resources:
    - name: page-component-template-versions-publicId
      path: /page-component-template-versions/{publicId}
      operations:
      - name: get
        method: GET
        description: PageComponentTemplateVersion
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: PageComponentTemplateVersion
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pagecomponenttemplateversion-rest
    port: 8080
    description: REST adapter for IXON API — PageComponentTemplateVersion. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/page-component-template-versions/{publicid}
      name: page-component-template-versions-publicid
      description: REST surface for page-component-template-versions-publicId.
      operations:
      - method: GET
        name: get
        description: PageComponentTemplateVersion
        call: ixon-pagecomponenttemplateversion.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: PageComponentTemplateVersion
        call: ixon-pagecomponenttemplateversion.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pagecomponenttemplateversion-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PageComponentTemplateVersion. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: pagecomponenttemplateversion
      description: PageComponentTemplateVersion
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-pagecomponenttemplateversion.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: pagecomponenttemplateversion-2
      description: PageComponentTemplateVersion
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-pagecomponenttemplateversion.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
