naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetLibraryInstanceList
  description: 'IXON API — AssetLibraryInstanceList. 2 operations. Lead operation: AssetLibraryInstanceList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetLibraryInstanceList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetlibraryinstancelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetLibraryInstanceList business capability. Self-contained, no shared references.
    resources:
    - name: assets-library-instances
      path: /assets/library/instances
      operations:
      - name: post
        method: POST
        description: AssetLibraryInstanceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: patch
        method: PATCH
        description: AssetLibraryInstanceList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetlibraryinstancelist-rest
    port: 8080
    description: REST adapter for IXON API — AssetLibraryInstanceList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/assets/library/instances
      name: assets-library-instances
      description: REST surface for assets-library-instances.
      operations:
      - method: POST
        name: post
        description: AssetLibraryInstanceList
        call: ixon-assetlibraryinstancelist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AssetLibraryInstanceList
        call: ixon-assetlibraryinstancelist.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetlibraryinstancelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetLibraryInstanceList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: assetlibraryinstancelist
      description: AssetLibraryInstanceList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-assetlibraryinstancelist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: assetlibraryinstancelist-2
      description: AssetLibraryInstanceList
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-assetlibraryinstancelist.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
