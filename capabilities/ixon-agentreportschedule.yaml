naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentReportSchedule
  description: 'IXON API — AgentReportSchedule. 3 operations. Lead operation: AgentReportSchedule. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentReportSchedule
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentreportschedule
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentReportSchedule business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-report-schedules-publicId
      path: /agents/{agentId}/report-schedules/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentReportSchedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentReportSchedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentReportSchedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentreportschedule-rest
    port: 8080
    description: REST adapter for IXON API — AgentReportSchedule. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/report-schedules/{publicid}
      name: agents-agentid-report-schedules-publicid
      description: REST surface for agents-agentId-report-schedules-publicId.
      operations:
      - method: GET
        name: get
        description: AgentReportSchedule
        call: ixon-agentreportschedule.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentReportSchedule
        call: ixon-agentreportschedule.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentReportSchedule
        call: ixon-agentreportschedule.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentreportschedule-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentReportSchedule. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentreportschedule
      description: AgentReportSchedule
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentreportschedule.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentreportschedule-2
      description: AgentReportSchedule
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentreportschedule.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentreportschedule-3
      description: AgentReportSchedule
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentreportschedule.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
