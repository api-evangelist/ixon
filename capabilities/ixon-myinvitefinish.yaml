naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyInviteFinish
  description: 'IXON API — MyInviteFinish. 1 operations. Lead operation: MyInviteFinish. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MyInviteFinish
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myinvitefinish
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyInviteFinish business capability. Self-contained, no shared references.
    resources:
    - name: invites-me-finish
      path: /invites/me/finish
      operations:
      - name: post
        method: POST
        description: MyInviteFinish
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myinvitefinish-rest
    port: 8080
    description: REST adapter for IXON API — MyInviteFinish. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/invites/me/finish
      name: invites-me-finish
      description: REST surface for invites-me-finish.
      operations:
      - method: POST
        name: post
        description: MyInviteFinish
        call: ixon-myinvitefinish.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myinvitefinish-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyInviteFinish. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: myinvitefinish
      description: MyInviteFinish
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myinvitefinish.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
