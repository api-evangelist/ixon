naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserInvite
  description: 'IXON API — MyUserInvite. 2 operations. Lead operation: MyUserInvite. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - MyUserInvite
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserinvite
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserInvite business capability. Self-contained, no shared references.
    resources:
    - name: users-me-invites-publicId
      path: /users/me/invites/{publicId}
      operations:
      - name: get
        method: GET
        description: MyUserInvite
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: delete
        method: DELETE
        description: MyUserInvite
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserinvite-rest
    port: 8080
    description: REST adapter for IXON API — MyUserInvite. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/me/invites/{publicid}
      name: users-me-invites-publicid
      description: REST surface for users-me-invites-publicId.
      operations:
      - method: GET
        name: get
        description: MyUserInvite
        call: ixon-myuserinvite.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MyUserInvite
        call: ixon-myuserinvite.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserinvite-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserInvite. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: myuserinvite
      description: MyUserInvite
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-myuserinvite.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: myuserinvite-2
      description: MyUserInvite
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-myuserinvite.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
