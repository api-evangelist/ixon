naftiko: 1.0.0-alpha2
info:
  label: IXON API — LicenceListExtend
  description: 'IXON API — LicenceListExtend. 1 operations. Lead operation: LicenceListExtend. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - LicenceListExtend
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-licencelistextend
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — LicenceListExtend business capability. Self-contained, no shared references.
    resources:
    - name: licences-extend
      path: /licences/extend
      operations:
      - name: post
        method: POST
        description: LicenceListExtend
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-licencelistextend-rest
    port: 8080
    description: REST adapter for IXON API — LicenceListExtend. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/licences/extend
      name: licences-extend
      description: REST surface for licences-extend.
      operations:
      - method: POST
        name: post
        description: LicenceListExtend
        call: ixon-licencelistextend.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-licencelistextend-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — LicenceListExtend. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: licencelistextend
      description: LicenceListExtend
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-licencelistextend.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
