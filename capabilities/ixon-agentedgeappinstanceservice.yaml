naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceService
  description: 'IXON API — AgentEdgeAppInstanceService. 2 operations. Lead operation: AgentEdgeAppInstanceService. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceService
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstanceservice
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceService business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances-services-publicId
      path: /agents/{agentId}/edge-app-instances/services/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentEdgeAppInstanceService
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentEdgeAppInstanceService
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstanceservice-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceService. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances/services/{publicid}
      name: agents-agentid-edge-app-instances-services-publicid
      description: REST surface for agents-agentId-edge-app-instances-services-publicId.
      operations:
      - method: GET
        name: get
        description: AgentEdgeAppInstanceService
        call: ixon-agentedgeappinstanceservice.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentEdgeAppInstanceService
        call: ixon-agentedgeappinstanceservice.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstanceservice-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceService. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentedgeappinstanceservice
      description: AgentEdgeAppInstanceService
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentedgeappinstanceservice.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentedgeappinstanceservice-2
      description: AgentEdgeAppInstanceService
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentedgeappinstanceservice.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
