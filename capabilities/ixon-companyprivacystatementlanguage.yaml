naftiko: 1.0.0-alpha2
info:
  label: IXON API — CompanyPrivacyStatementLanguage
  description: 'IXON API — CompanyPrivacyStatementLanguage. 1 operations. Lead operation: CompanyPrivacyStatementLanguage.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - CompanyPrivacyStatementLanguage
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-companyprivacystatementlanguage
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — CompanyPrivacyStatementLanguage business capability. Self-contained, no shared references.
    resources:
    - name: companies-privacy-statement-languages
      path: /companies/privacy-statement-languages
      operations:
      - name: patch
        method: PATCH
        description: CompanyPrivacyStatementLanguage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-companyprivacystatementlanguage-rest
    port: 8080
    description: REST adapter for IXON API — CompanyPrivacyStatementLanguage. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/companies/privacy-statement-languages
      name: companies-privacy-statement-languages
      description: REST surface for companies-privacy-statement-languages.
      operations:
      - method: PATCH
        name: patch
        description: CompanyPrivacyStatementLanguage
        call: ixon-companyprivacystatementlanguage.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-companyprivacystatementlanguage-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — CompanyPrivacyStatementLanguage. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: companyprivacystatementlanguage
      description: CompanyPrivacyStatementLanguage
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-companyprivacystatementlanguage.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
