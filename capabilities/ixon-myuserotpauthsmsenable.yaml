naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserOtpAuthSmsEnable
  description: 'IXON API — MyUserOtpAuthSmsEnable. 1 operations. Lead operation: MyUserOtpAuthSmsEnable. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserOtpAuthSmsEnable
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserotpauthsmsenable
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserOtpAuthSmsEnable business capability. Self-contained, no shared references.
    resources:
    - name: users-me-otp-auth-sms-enable
      path: /users/me/otp-auth-sms/enable
      operations:
      - name: post
        method: POST
        description: MyUserOtpAuthSmsEnable
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserotpauthsmsenable-rest
    port: 8080
    description: REST adapter for IXON API — MyUserOtpAuthSmsEnable. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/otp-auth-sms/enable
      name: users-me-otp-auth-sms-enable
      description: REST surface for users-me-otp-auth-sms-enable.
      operations:
      - method: POST
        name: post
        description: MyUserOtpAuthSmsEnable
        call: ixon-myuserotpauthsmsenable.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserotpauthsmsenable-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserOtpAuthSmsEnable. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: myuserotpauthsmsenable
      description: MyUserOtpAuthSmsEnable
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserotpauthsmsenable.post
      outputParameters:
      - type: object
        mapping: $.
