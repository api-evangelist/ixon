naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentTemplateShare
  description: 'IXON API — BackendComponentTemplateShare. 1 operations. Lead operation: BackendComponentTemplateShare. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentTemplateShare
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponenttemplateshare
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentTemplateShare business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-template-shares-publicId
      path: /backend-component-template-shares/{publicId}
      operations:
      - name: get
        method: GET
        description: BackendComponentTemplateShare
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponenttemplateshare-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentTemplateShare. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/backend-component-template-shares/{publicid}
      name: backend-component-template-shares-publicid
      description: REST surface for backend-component-template-shares-publicId.
      operations:
      - method: GET
        name: get
        description: BackendComponentTemplateShare
        call: ixon-backendcomponenttemplateshare.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponenttemplateshare-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentTemplateShare. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: backendcomponenttemplateshare
      description: BackendComponentTemplateShare
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-backendcomponenttemplateshare.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
