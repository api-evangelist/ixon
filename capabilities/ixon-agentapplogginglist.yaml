naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAppLoggingList
  description: 'IXON API — AgentAppLoggingList. 1 operations. Lead operation: AgentAppLoggingList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAppLoggingList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentapplogginglist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAppLoggingList business capability. Self-contained, no shared references.
    resources:
    - name: agents-app-logging
      path: /agents/app/logging
      operations:
      - name: delete
        method: DELETE
        description: AgentAppLoggingList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentapplogginglist-rest
    port: 8080
    description: REST adapter for IXON API — AgentAppLoggingList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/app/logging
      name: agents-app-logging
      description: REST surface for agents-app-logging.
      operations:
      - method: DELETE
        name: delete
        description: AgentAppLoggingList
        call: ixon-agentapplogginglist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentapplogginglist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAppLoggingList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentapplogginglist
      description: AgentAppLoggingList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentapplogginglist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
