naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterWanConfigWlanPush
  description: 'IXON API — AgentRouterWanConfigWlanPush. 2 operations. Lead operation: AgentRouterWanConfigWlanPush. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterWanConfigWlanPush
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentrouterwanconfigwlanpush
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterWanConfigWlanPush business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-wan-config-wlan
      path: /agents/{agentId}/router-wan-config/wlan
      operations:
      - name: post
        method: POST
        description: AgentRouterWanConfigWlanPush
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentRouterWanConfigWlanPush
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentrouterwanconfigwlanpush-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterWanConfigWlanPush. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-wan-config/wlan
      name: agents-agentid-router-wan-config-wlan
      description: REST surface for agents-agentId-router-wan-config-wlan.
      operations:
      - method: POST
        name: post
        description: AgentRouterWanConfigWlanPush
        call: ixon-agentrouterwanconfigwlanpush.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentRouterWanConfigWlanPush
        call: ixon-agentrouterwanconfigwlanpush.delete
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentrouterwanconfigwlanpush-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterWanConfigWlanPush. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentrouterwanconfigwlanpush
      description: AgentRouterWanConfigWlanPush
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentrouterwanconfigwlanpush.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouterwanconfigwlanpush-2
      description: AgentRouterWanConfigWlanPush
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentrouterwanconfigwlanpush.delete
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
