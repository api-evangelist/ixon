naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceDeleteList
  description: 'IXON API — AgentEdgeAppInstanceDeleteList. 1 operations. Lead operation: AgentEdgeAppInstanceDeleteList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceDeleteList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstancedeletelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceDeleteList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances-delete
      path: /agents/{agentId}/edge-app-instances/delete
      operations:
      - name: post
        method: POST
        description: AgentEdgeAppInstanceDeleteList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstancedeletelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceDeleteList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances/delete
      name: agents-agentid-edge-app-instances-delete
      description: REST surface for agents-agentId-edge-app-instances-delete.
      operations:
      - method: POST
        name: post
        description: AgentEdgeAppInstanceDeleteList
        call: ixon-agentedgeappinstancedeletelist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstancedeletelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceDeleteList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentedgeappinstancedeletelist
      description: AgentEdgeAppInstanceDeleteList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentedgeappinstancedeletelist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
