naftiko: 1.0.0-alpha2
info:
  label: IXON API — AuditLogList
  description: 'IXON API — AuditLogList. 1 operations. Lead operation: AuditLogList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AuditLogList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-auditloglist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AuditLogList business capability. Self-contained, no shared references.
    resources:
    - name: audit-logs
      path: /audit-logs
      operations:
      - name: get
        method: GET
        description: AuditLogList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-auditloglist-rest
    port: 8080
    description: REST adapter for IXON API — AuditLogList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/audit-logs
      name: audit-logs
      description: REST surface for audit-logs.
      operations:
      - method: GET
        name: get
        description: AuditLogList
        call: ixon-auditloglist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-auditloglist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AuditLogList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: auditloglist
      description: AuditLogList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-auditloglist.get
      outputParameters:
      - type: object
        mapping: $.
