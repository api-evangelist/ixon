naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentObjectStorageUploadAction
  description: 'IXON API — AgentObjectStorageUploadAction. 1 operations. Lead operation: AgentObjectStorageUploadAction. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentObjectStorageUploadAction
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentobjectstorageuploadaction
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentObjectStorageUploadAction business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-object-storage-upload
      path: /agents/{agentId}/object-storage/upload
      operations:
      - name: post
        method: POST
        description: AgentObjectStorageUploadAction
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentobjectstorageuploadaction-rest
    port: 8080
    description: REST adapter for IXON API — AgentObjectStorageUploadAction. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/object-storage/upload
      name: agents-agentid-object-storage-upload
      description: REST surface for agents-agentId-object-storage-upload.
      operations:
      - method: POST
        name: post
        description: AgentObjectStorageUploadAction
        call: ixon-agentobjectstorageuploadaction.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentobjectstorageuploadaction-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentObjectStorageUploadAction. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentobjectstorageuploadaction
      description: AgentObjectStorageUploadAction
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentobjectstorageuploadaction.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
