naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentTemplateVersionUpload
  description: 'IXON API — BackendComponentTemplateVersionUpload. 1 operations. Lead operation: BackendComponentTemplateVersionUpload.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentTemplateVersionUpload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponenttemplateversionupload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentTemplateVersionUpload business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-templates-publicId-version-upload
      path: /backend-component-templates/{publicId}/version-upload
      operations:
      - name: post
        method: POST
        description: BackendComponentTemplateVersionUpload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponenttemplateversionupload-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentTemplateVersionUpload. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/backend-component-templates/{publicid}/version-upload
      name: backend-component-templates-publicid-version-upload
      description: REST surface for backend-component-templates-publicId-version-upload.
      operations:
      - method: POST
        name: post
        description: BackendComponentTemplateVersionUpload
        call: ixon-backendcomponenttemplateversionupload.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponenttemplateversionupload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentTemplateVersionUpload. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: backendcomponenttemplateversionupload
      description: BackendComponentTemplateVersionUpload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-backendcomponenttemplateversionupload.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
