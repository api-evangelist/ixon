naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserPushDeviceClaim
  description: 'IXON API — MyUserPushDeviceClaim. 1 operations. Lead operation: MyUserPushDeviceClaim. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserPushDeviceClaim
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserpushdeviceclaim
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserPushDeviceClaim business capability. Self-contained, no shared references.
    resources:
    - name: users-me-push-devices-claim
      path: /users/me/push-devices/claim
      operations:
      - name: post
        method: POST
        description: MyUserPushDeviceClaim
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserpushdeviceclaim-rest
    port: 8080
    description: REST adapter for IXON API — MyUserPushDeviceClaim. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/push-devices/claim
      name: users-me-push-devices-claim
      description: REST surface for users-me-push-devices-claim.
      operations:
      - method: POST
        name: post
        description: MyUserPushDeviceClaim
        call: ixon-myuserpushdeviceclaim.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserpushdeviceclaim-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserPushDeviceClaim. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: myuserpushdeviceclaim
      description: MyUserPushDeviceClaim
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuserpushdeviceclaim.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
