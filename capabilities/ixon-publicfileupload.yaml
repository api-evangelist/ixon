naftiko: 1.0.0-alpha2
info:
  label: IXON API — PublicFileUpload
  description: 'IXON API — PublicFileUpload. 1 operations. Lead operation: PublicFileUpload. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - PublicFileUpload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-publicfileupload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PublicFileUpload business capability. Self-contained, no shared references.
    resources:
    - name: public-files-upload
      path: /public-files/upload
      operations:
      - name: post
        method: POST
        description: PublicFileUpload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-publicfileupload-rest
    port: 8080
    description: REST adapter for IXON API — PublicFileUpload. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/public-files/upload
      name: public-files-upload
      description: REST surface for public-files-upload.
      operations:
      - method: POST
        name: post
        description: PublicFileUpload
        call: ixon-publicfileupload.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-publicfileupload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PublicFileUpload. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: publicfileupload
      description: PublicFileUpload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-publicfileupload.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
