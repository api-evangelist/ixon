naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentTemplateConfig
  description: 'IXON API — BackendComponentTemplateConfig. 2 operations. Lead operation: BackendComponentTemplateConfig. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentTemplateConfig
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponenttemplateconfig
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentTemplateConfig business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-template-configs-publicId
      path: /backend-component-template-configs/{publicId}
      operations:
      - name: get
        method: GET
        description: BackendComponentTemplateConfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: BackendComponentTemplateConfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponenttemplateconfig-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentTemplateConfig. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/backend-component-template-configs/{publicid}
      name: backend-component-template-configs-publicid
      description: REST surface for backend-component-template-configs-publicId.
      operations:
      - method: GET
        name: get
        description: BackendComponentTemplateConfig
        call: ixon-backendcomponenttemplateconfig.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: BackendComponentTemplateConfig
        call: ixon-backendcomponenttemplateconfig.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponenttemplateconfig-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentTemplateConfig. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: backendcomponenttemplateconfig
      description: BackendComponentTemplateConfig
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-backendcomponenttemplateconfig.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: backendcomponenttemplateconfig-2
      description: BackendComponentTemplateConfig
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-backendcomponenttemplateconfig.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
