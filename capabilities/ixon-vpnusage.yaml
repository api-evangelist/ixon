naftiko: 1.0.0-alpha2
info:
  label: IXON API — VpnUsage
  description: 'IXON API — VpnUsage. 1 operations. Lead operation: VpnUsage. Self-contained Naftiko capability covering one
    Ixon business surface.'
  tags:
  - Ixon
  - VpnUsage
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-vpnusage
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — VpnUsage business capability. Self-contained, no shared references.
    resources:
    - name: vpn-usage
      path: /vpn-usage
      operations:
      - name: post
        method: POST
        description: VpnUsage
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-vpnusage-rest
    port: 8080
    description: REST adapter for IXON API — VpnUsage. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/vpn-usage
      name: vpn-usage
      description: REST surface for vpn-usage.
      operations:
      - method: POST
        name: post
        description: VpnUsage
        call: ixon-vpnusage.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-vpnusage-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — VpnUsage. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: vpnusage
      description: VpnUsage
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-vpnusage.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
