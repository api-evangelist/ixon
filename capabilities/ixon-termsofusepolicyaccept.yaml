naftiko: 1.0.0-alpha2
info:
  label: IXON API — TermsOfUsePolicyAccept
  description: 'IXON API — TermsOfUsePolicyAccept. 1 operations. Lead operation: TermsOfUsePolicyAccept. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - TermsOfUsePolicyAccept
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-termsofusepolicyaccept
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — TermsOfUsePolicyAccept business capability. Self-contained, no shared references.
    resources:
    - name: terms-of-use-policies-publicId-accept
      path: /terms-of-use-policies/{publicId}/accept
      operations:
      - name: post
        method: POST
        description: TermsOfUsePolicyAccept
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-termsofusepolicyaccept-rest
    port: 8080
    description: REST adapter for IXON API — TermsOfUsePolicyAccept. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/terms-of-use-policies/{publicid}/accept
      name: terms-of-use-policies-publicid-accept
      description: REST surface for terms-of-use-policies-publicId-accept.
      operations:
      - method: POST
        name: post
        description: TermsOfUsePolicyAccept
        call: ixon-termsofusepolicyaccept.post
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-termsofusepolicyaccept-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — TermsOfUsePolicyAccept. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: termsofusepolicyaccept
      description: TermsOfUsePolicyAccept
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-termsofusepolicyaccept.post
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
