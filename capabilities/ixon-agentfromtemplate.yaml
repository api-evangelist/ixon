naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentFromTemplate
  description: 'IXON API — AgentFromTemplate. 1 operations. Lead operation: AgentFromTemplate. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentFromTemplate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentfromtemplate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentFromTemplate business capability. Self-contained, no shared references.
    resources:
    - name: agents-publicId-from-template
      path: /agents/{publicId}/from-template
      operations:
      - name: patch
        method: PATCH
        description: AgentFromTemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentfromtemplate-rest
    port: 8080
    description: REST adapter for IXON API — AgentFromTemplate. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{publicid}/from-template
      name: agents-publicid-from-template
      description: REST surface for agents-publicId-from-template.
      operations:
      - method: PATCH
        name: patch
        description: AgentFromTemplate
        call: ixon-agentfromtemplate.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentfromtemplate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentFromTemplate. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentfromtemplate
      description: AgentFromTemplate
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentfromtemplate.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
