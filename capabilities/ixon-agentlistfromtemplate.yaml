naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentListFromTemplate
  description: 'IXON API — AgentListFromTemplate. 1 operations. Lead operation: AgentListFromTemplate. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentListFromTemplate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlistfromtemplate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentListFromTemplate business capability. Self-contained, no shared references.
    resources:
    - name: agents-from-template
      path: /agents/from-template
      operations:
      - name: patch
        method: PATCH
        description: AgentListFromTemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlistfromtemplate-rest
    port: 8080
    description: REST adapter for IXON API — AgentListFromTemplate. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/from-template
      name: agents-from-template
      description: REST surface for agents-from-template.
      operations:
      - method: PATCH
        name: patch
        description: AgentListFromTemplate
        call: ixon-agentlistfromtemplate.patch
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlistfromtemplate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentListFromTemplate. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentlistfromtemplate
      description: AgentListFromTemplate
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentlistfromtemplate.patch
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
