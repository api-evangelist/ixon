naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterVpnConfig
  description: 'IXON API — AgentRouterVpnConfig. 1 operations. Lead operation: AgentRouterVpnConfig. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterVpnConfig
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentroutervpnconfig
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterVpnConfig business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-vpn-config
      path: /agents/{agentId}/router-vpn-config
      operations:
      - name: patch
        method: PATCH
        description: AgentRouterVpnConfig
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentroutervpnconfig-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterVpnConfig. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-vpn-config
      name: agents-agentid-router-vpn-config
      description: REST surface for agents-agentId-router-vpn-config.
      operations:
      - method: PATCH
        name: patch
        description: AgentRouterVpnConfig
        call: ixon-agentroutervpnconfig.patch
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentroutervpnconfig-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterVpnConfig. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentroutervpnconfig
      description: AgentRouterVpnConfig
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentroutervpnconfig.patch
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
