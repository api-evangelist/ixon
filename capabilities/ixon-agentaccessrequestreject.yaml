naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentAccessRequestReject
  description: 'IXON API — AgentAccessRequestReject. 1 operations. Lead operation: AgentAccessRequestReject. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentAccessRequestReject
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentaccessrequestreject
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentAccessRequestReject business capability. Self-contained, no shared references.
    resources:
    - name: agents-access-requests-reject
      path: /agents/access-requests/reject
      operations:
      - name: post
        method: POST
        description: AgentAccessRequestReject
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentaccessrequestreject-rest
    port: 8080
    description: REST adapter for IXON API — AgentAccessRequestReject. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/access-requests/reject
      name: agents-access-requests-reject
      description: REST surface for agents-access-requests-reject.
      operations:
      - method: POST
        name: post
        description: AgentAccessRequestReject
        call: ixon-agentaccessrequestreject.post
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentaccessrequestreject-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentAccessRequestReject. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentaccessrequestreject
      description: AgentAccessRequestReject
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentaccessrequestreject.post
      outputParameters:
      - type: object
        mapping: $.
