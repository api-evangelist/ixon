naftiko: 1.0.0-alpha2
info:
  label: IXON API — WebhookSubscription
  description: 'IXON API — WebhookSubscription. 3 operations. Lead operation: WebhookSubscription. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - WebhookSubscription
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-webhooksubscription
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — WebhookSubscription business capability. Self-contained, no shared references.
    resources:
    - name: webhook-subscriptions-publicId
      path: /webhook-subscriptions/{publicId}
      operations:
      - name: get
        method: GET
        description: WebhookSubscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: WebhookSubscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: WebhookSubscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-webhooksubscription-rest
    port: 8080
    description: REST adapter for IXON API — WebhookSubscription. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/webhook-subscriptions/{publicid}
      name: webhook-subscriptions-publicid
      description: REST surface for webhook-subscriptions-publicId.
      operations:
      - method: GET
        name: get
        description: WebhookSubscription
        call: ixon-webhooksubscription.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: WebhookSubscription
        call: ixon-webhooksubscription.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: WebhookSubscription
        call: ixon-webhooksubscription.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-webhooksubscription-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — WebhookSubscription. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: webhooksubscription
      description: WebhookSubscription
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-webhooksubscription.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: webhooksubscription-2
      description: WebhookSubscription
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-webhooksubscription.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: webhooksubscription-3
      description: WebhookSubscription
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-webhooksubscription.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
