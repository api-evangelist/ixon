naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentFirmwareUpgrade
  description: 'IXON API — AgentFirmwareUpgrade. 1 operations. Lead operation: AgentFirmwareUpgrade. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentFirmwareUpgrade
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentfirmwareupgrade
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentFirmwareUpgrade business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-firmware-upgrade
      path: /agents/{agentId}/firmware-upgrade
      operations:
      - name: post
        method: POST
        description: AgentFirmwareUpgrade
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentfirmwareupgrade-rest
    port: 8080
    description: REST adapter for IXON API — AgentFirmwareUpgrade. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/firmware-upgrade
      name: agents-agentid-firmware-upgrade
      description: REST surface for agents-agentId-firmware-upgrade.
      operations:
      - method: POST
        name: post
        description: AgentFirmwareUpgrade
        call: ixon-agentfirmwareupgrade.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentfirmwareupgrade-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentFirmwareUpgrade. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentfirmwareupgrade
      description: AgentFirmwareUpgrade
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentfirmwareupgrade.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
