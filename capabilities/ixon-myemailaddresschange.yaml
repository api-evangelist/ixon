naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyEmailAddressChange
  description: 'IXON API — MyEmailAddressChange. 2 operations. Lead operation: MyEmailAddressChange. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyEmailAddressChange
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myemailaddresschange
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyEmailAddressChange business capability. Self-contained, no shared references.
    resources:
    - name: email-address-changes-me
      path: /email-address-changes/me
      operations:
      - name: get
        method: GET
        description: MyEmailAddressChange
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: delete
        method: DELETE
        description: MyEmailAddressChange
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myemailaddresschange-rest
    port: 8080
    description: REST adapter for IXON API — MyEmailAddressChange. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/email-address-changes/me
      name: email-address-changes-me
      description: REST surface for email-address-changes-me.
      operations:
      - method: GET
        name: get
        description: MyEmailAddressChange
        call: ixon-myemailaddresschange.get
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MyEmailAddressChange
        call: ixon-myemailaddresschange.delete
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myemailaddresschange-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyEmailAddressChange. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myemailaddresschange
      description: MyEmailAddressChange
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-myemailaddresschange.get
      outputParameters:
      - type: object
        mapping: $.
    - name: myemailaddresschange-2
      description: MyEmailAddressChange
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-myemailaddresschange.delete
      outputParameters:
      - type: object
        mapping: $.
