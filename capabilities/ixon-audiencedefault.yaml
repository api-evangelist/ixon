naftiko: 1.0.0-alpha2
info:
  label: IXON API — AudienceDefault
  description: 'IXON API — AudienceDefault. 2 operations. Lead operation: AudienceDefault. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AudienceDefault
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-audiencedefault
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AudienceDefault business capability. Self-contained, no shared references.
    resources:
    - name: audiences-publicId-default
      path: /audiences/{publicId}/default
      operations:
      - name: post
        method: POST
        description: AudienceDefault
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: delete
        method: DELETE
        description: AudienceDefault
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-audiencedefault-rest
    port: 8080
    description: REST adapter for IXON API — AudienceDefault. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/audiences/{publicid}/default
      name: audiences-publicid-default
      description: REST surface for audiences-publicId-default.
      operations:
      - method: POST
        name: post
        description: AudienceDefault
        call: ixon-audiencedefault.post
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AudienceDefault
        call: ixon-audiencedefault.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-audiencedefault-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AudienceDefault. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: audiencedefault
      description: AudienceDefault
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-audiencedefault.post
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: audiencedefault-2
      description: AudienceDefault
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-audiencedefault.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
