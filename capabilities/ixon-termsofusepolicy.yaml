naftiko: 1.0.0-alpha2
info:
  label: IXON API — TermsOfUsePolicy
  description: 'IXON API — TermsOfUsePolicy. 1 operations. Lead operation: TermsOfUsePolicy. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - TermsOfUsePolicy
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-termsofusepolicy
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — TermsOfUsePolicy business capability. Self-contained, no shared references.
    resources:
    - name: terms-of-use-policies-publicId
      path: /terms-of-use-policies/{publicId}
      operations:
      - name: get
        method: GET
        description: TermsOfUsePolicy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-termsofusepolicy-rest
    port: 8080
    description: REST adapter for IXON API — TermsOfUsePolicy. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/terms-of-use-policies/{publicid}
      name: terms-of-use-policies-publicid
      description: REST surface for terms-of-use-policies-publicId.
      operations:
      - method: GET
        name: get
        description: TermsOfUsePolicy
        call: ixon-termsofusepolicy.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-termsofusepolicy-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — TermsOfUsePolicy. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: termsofusepolicy
      description: TermsOfUsePolicy
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-termsofusepolicy.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
