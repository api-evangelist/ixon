naftiko: 1.0.0-alpha2
info:
  label: IXON API — PlatformServerList
  description: 'IXON API — PlatformServerList. 1 operations. Lead operation: PlatformServerList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - PlatformServerList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-platformserverlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PlatformServerList business capability. Self-contained, no shared references.
    resources:
    - name: platform-servers
      path: /platform-servers
      operations:
      - name: get
        method: GET
        description: PlatformServerList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-platformserverlist-rest
    port: 8080
    description: REST adapter for IXON API — PlatformServerList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/platform-servers
      name: platform-servers
      description: REST surface for platform-servers.
      operations:
      - method: GET
        name: get
        description: PlatformServerList
        call: ixon-platformserverlist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-platformserverlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PlatformServerList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: platformserverlist
      description: PlatformServerList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-platformserverlist.get
      outputParameters:
      - type: object
        mapping: $.
