naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceFileUpload
  description: 'IXON API — AgentEdgeAppInstanceFileUpload. 1 operations. Lead operation: AgentEdgeAppInstanceFileUpload. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceFileUpload
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstancefileupload
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceFileUpload business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances-instanceId-files-upload
      path: /agents/{agentId}/edge-app-instances/{instanceId}/files/upload
      operations:
      - name: post
        method: POST
        description: AgentEdgeAppInstanceFileUpload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: instanceId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstancefileupload-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceFileUpload. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances/{instanceid}/files/upload
      name: agents-agentid-edge-app-instances-instanceid-files-upload
      description: REST surface for agents-agentId-edge-app-instances-instanceId-files-upload.
      operations:
      - method: POST
        name: post
        description: AgentEdgeAppInstanceFileUpload
        call: ixon-agentedgeappinstancefileupload.post
        with:
          agentId: rest.agentId
          instanceId: rest.instanceId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstancefileupload-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceFileUpload. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentedgeappinstancefileupload
      description: AgentEdgeAppInstanceFileUpload
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentedgeappinstancefileupload.post
      with:
        agentId: tools.agentId
        instanceId: tools.instanceId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
