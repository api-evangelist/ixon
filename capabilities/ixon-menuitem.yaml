naftiko: 1.0.0-alpha2
info:
  label: IXON API — MenuItem
  description: 'IXON API — MenuItem. 3 operations. Lead operation: MenuItem. Self-contained Naftiko capability covering one
    Ixon business surface.'
  tags:
  - Ixon
  - MenuItem
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-menuitem
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MenuItem business capability. Self-contained, no shared references.
    resources:
    - name: menu-items-publicId
      path: /menu-items/{publicId}
      operations:
      - name: get
        method: GET
        description: MenuItem
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: MenuItem
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: MenuItem
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-menuitem-rest
    port: 8080
    description: REST adapter for IXON API — MenuItem. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/menu-items/{publicid}
      name: menu-items-publicid
      description: REST surface for menu-items-publicId.
      operations:
      - method: GET
        name: get
        description: MenuItem
        call: ixon-menuitem.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: MenuItem
        call: ixon-menuitem.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MenuItem
        call: ixon-menuitem.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-menuitem-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MenuItem. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: menuitem
      description: MenuItem
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-menuitem.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: menuitem-2
      description: MenuItem
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-menuitem.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: menuitem-3
      description: MenuItem
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-menuitem.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
