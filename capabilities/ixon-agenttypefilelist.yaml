naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentTypeFileList
  description: 'IXON API — AgentTypeFileList. 1 operations. Lead operation: AgentTypeFileList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentTypeFileList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agenttypefilelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentTypeFileList business capability. Self-contained, no shared references.
    resources:
    - name: agent-types-publicId-files
      path: /agent-types/{publicId}/files
      operations:
      - name: get
        method: GET
        description: AgentTypeFileList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agenttypefilelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentTypeFileList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agent-types/{publicid}/files
      name: agent-types-publicid-files
      description: REST surface for agent-types-publicId-files.
      operations:
      - method: GET
        name: get
        description: AgentTypeFileList
        call: ixon-agenttypefilelist.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agenttypefilelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentTypeFileList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agenttypefilelist
      description: AgentTypeFileList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agenttypefilelist.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
