naftiko: 1.0.0-alpha2
info:
  label: IXON API — UserZoneGrantsList
  description: 'IXON API — UserZoneGrantsList. 1 operations. Lead operation: UserZoneGrantsList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - UserZoneGrantsList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-userzonegrantslist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — UserZoneGrantsList business capability. Self-contained, no shared references.
    resources:
    - name: user-zone-grants
      path: /user-zone-grants
      operations:
      - name: get
        method: GET
        description: UserZoneGrantsList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-userzonegrantslist-rest
    port: 8080
    description: REST adapter for IXON API — UserZoneGrantsList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/user-zone-grants
      name: user-zone-grants
      description: REST surface for user-zone-grants.
      operations:
      - method: GET
        name: get
        description: UserZoneGrantsList
        call: ixon-userzonegrantslist.get
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-userzonegrantslist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — UserZoneGrantsList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: userzonegrantslist
      description: UserZoneGrantsList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-userzonegrantslist.get
      outputParameters:
      - type: object
        mapping: $.
