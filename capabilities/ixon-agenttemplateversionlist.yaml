naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentTemplateVersionList
  description: 'IXON API — AgentTemplateVersionList. 1 operations. Lead operation: AgentTemplateVersionList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentTemplateVersionList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agenttemplateversionlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentTemplateVersionList business capability. Self-contained, no shared references.
    resources:
    - name: agent-templates-agentId-versions
      path: /agent-templates/{agentId}/versions
      operations:
      - name: get
        method: GET
        description: AgentTemplateVersionList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agenttemplateversionlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentTemplateVersionList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agent-templates/{agentid}/versions
      name: agent-templates-agentid-versions
      description: REST surface for agent-templates-agentId-versions.
      operations:
      - method: GET
        name: get
        description: AgentTemplateVersionList
        call: ixon-agenttemplateversionlist.get
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agenttemplateversionlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentTemplateVersionList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agenttemplateversionlist
      description: AgentTemplateVersionList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agenttemplateversionlist.get
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
