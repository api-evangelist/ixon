naftiko: 1.0.0-alpha2
info:
  label: IXON API — PageDuplicate
  description: 'IXON API — PageDuplicate. 1 operations. Lead operation: PageDuplicate. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - PageDuplicate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pageduplicate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PageDuplicate business capability. Self-contained, no shared references.
    resources:
    - name: pages-publicId-duplicate
      path: /pages/{publicId}/duplicate
      operations:
      - name: post
        method: POST
        description: PageDuplicate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pageduplicate-rest
    port: 8080
    description: REST adapter for IXON API — PageDuplicate. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/pages/{publicid}/duplicate
      name: pages-publicid-duplicate
      description: REST surface for pages-publicId-duplicate.
      operations:
      - method: POST
        name: post
        description: PageDuplicate
        call: ixon-pageduplicate.post
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pageduplicate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PageDuplicate. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: pageduplicate
      description: PageDuplicate
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-pageduplicate.post
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
