naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentVpnUsageList
  description: 'IXON API — AgentVpnUsageList. 1 operations. Lead operation: AgentVpnUsageList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentVpnUsageList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentvpnusagelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentVpnUsageList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-vpn-usage-2
      path: /agents/{agentId}/vpn-usage-2
      operations:
      - name: post
        method: POST
        description: AgentVpnUsageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentvpnusagelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentVpnUsageList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/vpn-usage-2
      name: agents-agentid-vpn-usage-2
      description: REST surface for agents-agentId-vpn-usage-2.
      operations:
      - method: POST
        name: post
        description: AgentVpnUsageList
        call: ixon-agentvpnusagelist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentvpnusagelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentVpnUsageList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentvpnusagelist
      description: AgentVpnUsageList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentvpnusagelist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
