naftiko: 1.0.0-alpha2
info:
  label: IXON API — PipelineFunctionList
  description: 'IXON API — PipelineFunctionList. 2 operations. Lead operation: PipelineFunctionList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - PipelineFunctionList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-pipelinefunctionlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — PipelineFunctionList business capability. Self-contained, no shared references.
    resources:
    - name: pipeline-functions
      path: /pipeline-functions
      operations:
      - name: get
        method: GET
        description: PipelineFunctionList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: PipelineFunctionList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-pipelinefunctionlist-rest
    port: 8080
    description: REST adapter for IXON API — PipelineFunctionList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/pipeline-functions
      name: pipeline-functions
      description: REST surface for pipeline-functions.
      operations:
      - method: GET
        name: get
        description: PipelineFunctionList
        call: ixon-pipelinefunctionlist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: PipelineFunctionList
        call: ixon-pipelinefunctionlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-pipelinefunctionlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — PipelineFunctionList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: pipelinefunctionlist
      description: PipelineFunctionList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-pipelinefunctionlist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: pipelinefunctionlist-2
      description: PipelineFunctionList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-pipelinefunctionlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
