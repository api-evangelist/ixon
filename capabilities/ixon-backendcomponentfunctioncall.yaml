naftiko: 1.0.0-alpha2
info:
  label: IXON API — BackendComponentFunctionCall
  description: 'IXON API — BackendComponentFunctionCall. 1 operations. Lead operation: BackendComponentFunctionCall. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - BackendComponentFunctionCall
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-backendcomponentfunctioncall
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — BackendComponentFunctionCall business capability. Self-contained, no shared references.
    resources:
    - name: backend-component-function-call
      path: /backend-component-function-call
      operations:
      - name: post
        method: POST
        description: BackendComponentFunctionCall
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-backendcomponentfunctioncall-rest
    port: 8080
    description: REST adapter for IXON API — BackendComponentFunctionCall. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/backend-component-function-call
      name: backend-component-function-call
      description: REST surface for backend-component-function-call.
      operations:
      - method: POST
        name: post
        description: BackendComponentFunctionCall
        call: ixon-backendcomponentfunctioncall.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-backendcomponentfunctioncall-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — BackendComponentFunctionCall. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: backendcomponentfunctioncall
      description: BackendComponentFunctionCall
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-backendcomponentfunctioncall.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
