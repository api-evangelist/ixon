naftiko: 1.0.0-alpha2
info:
  label: IXON API — QueueReportList
  description: 'IXON API — QueueReportList. 1 operations. Lead operation: QueueReportList. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - QueueReportList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-queuereportlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — QueueReportList business capability. Self-contained, no shared references.
    resources:
    - name: pages-report-queue
      path: /pages/report/queue
      operations:
      - name: post
        method: POST
        description: QueueReportList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-queuereportlist-rest
    port: 8080
    description: REST adapter for IXON API — QueueReportList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/pages/report/queue
      name: pages-report-queue
      description: REST surface for pages-report-queue.
      operations:
      - method: POST
        name: post
        description: QueueReportList
        call: ixon-queuereportlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-queuereportlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — QueueReportList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: queuereportlist
      description: QueueReportList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-queuereportlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
