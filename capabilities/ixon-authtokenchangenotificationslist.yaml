naftiko: 1.0.0-alpha2
info:
  label: IXON API — AuthTokenChangeNotificationsList
  description: 'IXON API — AuthTokenChangeNotificationsList. 1 operations. Lead operation: AuthTokenChangeNotificationsList.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AuthTokenChangeNotificationsList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-authtokenchangenotificationslist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AuthTokenChangeNotificationsList business capability. Self-contained, no shared references.
    resources:
    - name: auth-tokens-cns
      path: /auth-tokens/cns
      operations:
      - name: post
        method: POST
        description: AuthTokenChangeNotificationsList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-authtokenchangenotificationslist-rest
    port: 8080
    description: REST adapter for IXON API — AuthTokenChangeNotificationsList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/auth-tokens/cns
      name: auth-tokens-cns
      description: REST surface for auth-tokens-cns.
      operations:
      - method: POST
        name: post
        description: AuthTokenChangeNotificationsList
        call: ixon-authtokenchangenotificationslist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-authtokenchangenotificationslist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AuthTokenChangeNotificationsList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: authtokenchangenotificationslist
      description: AuthTokenChangeNotificationsList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-authtokenchangenotificationslist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
