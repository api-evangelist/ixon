naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentServerFromTemplateList
  description: 'IXON API — AgentServerFromTemplateList. 1 operations. Lead operation: AgentServerFromTemplateList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentServerFromTemplateList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentserverfromtemplatelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentServerFromTemplateList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-servers-from-template
      path: /agents/{agentId}/servers/from-template
      operations:
      - name: post
        method: POST
        description: AgentServerFromTemplateList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentserverfromtemplatelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentServerFromTemplateList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/servers/from-template
      name: agents-agentid-servers-from-template
      description: REST surface for agents-agentId-servers-from-template.
      operations:
      - method: POST
        name: post
        description: AgentServerFromTemplateList
        call: ixon-agentserverfromtemplatelist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentserverfromtemplatelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentServerFromTemplateList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentserverfromtemplatelist
      description: AgentServerFromTemplateList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentserverfromtemplatelist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
