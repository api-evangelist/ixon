naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyAccessRecover
  description: 'IXON API — MyAccessRecover. 2 operations. Lead operation: MyAccessRecover. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MyAccessRecover
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myaccessrecover
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyAccessRecover business capability. Self-contained, no shared references.
    resources:
    - name: access-recover-me
      path: /access-recover/me
      operations:
      - name: get
        method: GET
        description: MyAccessRecover
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: delete
        method: DELETE
        description: MyAccessRecover
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myaccessrecover-rest
    port: 8080
    description: REST adapter for IXON API — MyAccessRecover. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/access-recover/me
      name: access-recover-me
      description: REST surface for access-recover-me.
      operations:
      - method: GET
        name: get
        description: MyAccessRecover
        call: ixon-myaccessrecover.get
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: MyAccessRecover
        call: ixon-myaccessrecover.delete
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myaccessrecover-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyAccessRecover. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: myaccessrecover
      description: MyAccessRecover
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-myaccessrecover.get
      outputParameters:
      - type: object
        mapping: $.
    - name: myaccessrecover-2
      description: MyAccessRecover
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-myaccessrecover.delete
      outputParameters:
      - type: object
        mapping: $.
