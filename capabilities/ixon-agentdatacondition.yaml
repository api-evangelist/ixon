naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentDataCondition
  description: 'IXON API — AgentDataCondition. 3 operations. Lead operation: AgentDataCondition. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentDataCondition
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentdatacondition
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentDataCondition business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-data-conditions-publicId
      path: /agents/{agentId}/data-conditions/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentDataCondition
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentDataCondition
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentDataCondition
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentdatacondition-rest
    port: 8080
    description: REST adapter for IXON API — AgentDataCondition. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agents/{agentid}/data-conditions/{publicid}
      name: agents-agentid-data-conditions-publicid
      description: REST surface for agents-agentId-data-conditions-publicId.
      operations:
      - method: GET
        name: get
        description: AgentDataCondition
        call: ixon-agentdatacondition.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentDataCondition
        call: ixon-agentdatacondition.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentDataCondition
        call: ixon-agentdatacondition.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentdatacondition-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentDataCondition. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentdatacondition
      description: AgentDataCondition
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentdatacondition.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdatacondition-2
      description: AgentDataCondition
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentdatacondition.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentdatacondition-3
      description: AgentDataCondition
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentdatacondition.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
