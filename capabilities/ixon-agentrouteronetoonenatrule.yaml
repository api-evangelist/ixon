naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentRouterOneToOneNatRule
  description: 'IXON API — AgentRouterOneToOneNatRule. 3 operations. Lead operation: AgentRouterOneToOneNatRule. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentRouterOneToOneNatRule
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentrouteronetoonenatrule
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentRouterOneToOneNatRule business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-router-one-to-one-nat-rules-publicId
      path: /agents/{agentId}/router-one-to-one-nat-rules/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentRouterOneToOneNatRule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentRouterOneToOneNatRule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentRouterOneToOneNatRule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentrouteronetoonenatrule-rest
    port: 8080
    description: REST adapter for IXON API — AgentRouterOneToOneNatRule. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/router-one-to-one-nat-rules/{publicid}
      name: agents-agentid-router-one-to-one-nat-rules-publicid
      description: REST surface for agents-agentId-router-one-to-one-nat-rules-publicId.
      operations:
      - method: GET
        name: get
        description: AgentRouterOneToOneNatRule
        call: ixon-agentrouteronetoonenatrule.get
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentRouterOneToOneNatRule
        call: ixon-agentrouteronetoonenatrule.patch
        with:
          agentId: rest.agentId
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentRouterOneToOneNatRule
        call: ixon-agentrouteronetoonenatrule.delete
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentrouteronetoonenatrule-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentRouterOneToOneNatRule. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentrouteronetoonenatrule
      description: AgentRouterOneToOneNatRule
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentrouteronetoonenatrule.get
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouteronetoonenatrule-2
      description: AgentRouterOneToOneNatRule
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agentrouteronetoonenatrule.patch
      with:
        agentId: tools.agentId
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agentrouteronetoonenatrule-3
      description: AgentRouterOneToOneNatRule
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentrouteronetoonenatrule.delete
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
