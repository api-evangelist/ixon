naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentQueueReportList
  description: 'IXON API — AgentQueueReportList. 1 operations. Lead operation: AgentQueueReportList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentQueueReportList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentqueuereportlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentQueueReportList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-report-queue
      path: /agents/{agentId}/report/queue
      operations:
      - name: post
        method: POST
        description: AgentQueueReportList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentqueuereportlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentQueueReportList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/report/queue
      name: agents-agentid-report-queue
      description: REST surface for agents-agentId-report-queue.
      operations:
      - method: POST
        name: post
        description: AgentQueueReportList
        call: ixon-agentqueuereportlist.post
        with:
          agentId: rest.agentId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentqueuereportlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentQueueReportList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: agentqueuereportlist
      description: AgentQueueReportList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentqueuereportlist.post
      with:
        agentId: tools.agentId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
