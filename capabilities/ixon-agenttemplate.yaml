naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentTemplate
  description: 'IXON API — AgentTemplate. 3 operations. Lead operation: AgentTemplate. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - AgentTemplate
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agenttemplate
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentTemplate business capability. Self-contained, no shared references.
    resources:
    - name: agent-templates-publicId
      path: /agent-templates/{publicId}
      operations:
      - name: get
        method: GET
        description: AgentTemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
      - name: patch
        method: PATCH
        description: AgentTemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: delete
        method: DELETE
        description: AgentTemplate
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agenttemplate-rest
    port: 8080
    description: REST adapter for IXON API — AgentTemplate. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/agent-templates/{publicid}
      name: agent-templates-publicid
      description: REST surface for agent-templates-publicId.
      operations:
      - method: GET
        name: get
        description: AgentTemplate
        call: ixon-agenttemplate.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: patch
        description: AgentTemplate
        call: ixon-agenttemplate.patch
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: delete
        description: AgentTemplate
        call: ixon-agenttemplate.delete
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agenttemplate-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentTemplate. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: agenttemplate
      description: AgentTemplate
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agenttemplate.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
    - name: agenttemplate-2
      description: AgentTemplate
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: ixon-agenttemplate.patch
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: agenttemplate-3
      description: AgentTemplate
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agenttemplate.delete
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
