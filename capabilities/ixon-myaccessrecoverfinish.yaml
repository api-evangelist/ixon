naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyAccessRecoverFinish
  description: 'IXON API — MyAccessRecoverFinish. 1 operations. Lead operation: MyAccessRecoverFinish. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyAccessRecoverFinish
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myaccessrecoverfinish
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyAccessRecoverFinish business capability. Self-contained, no shared references.
    resources:
    - name: access-recover-me-finish
      path: /access-recover/me/finish
      operations:
      - name: post
        method: POST
        description: MyAccessRecoverFinish
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myaccessrecoverfinish-rest
    port: 8080
    description: REST adapter for IXON API — MyAccessRecoverFinish. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/access-recover/me/finish
      name: access-recover-me-finish
      description: REST surface for access-recover-me-finish.
      operations:
      - method: POST
        name: post
        description: MyAccessRecoverFinish
        call: ixon-myaccessrecoverfinish.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myaccessrecoverfinish-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyAccessRecoverFinish. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: myaccessrecoverfinish
      description: MyAccessRecoverFinish
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myaccessrecoverfinish.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
