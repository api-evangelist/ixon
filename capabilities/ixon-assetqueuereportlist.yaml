naftiko: 1.0.0-alpha2
info:
  label: IXON API — AssetQueueReportList
  description: 'IXON API — AssetQueueReportList. 1 operations. Lead operation: AssetQueueReportList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AssetQueueReportList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-assetqueuereportlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AssetQueueReportList business capability. Self-contained, no shared references.
    resources:
    - name: assets-assetId-report-queue
      path: /assets/{assetId}/report/queue
      operations:
      - name: post
        method: POST
        description: AssetQueueReportList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assetId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-assetqueuereportlist-rest
    port: 8080
    description: REST adapter for IXON API — AssetQueueReportList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/assets/{assetid}/report/queue
      name: assets-assetid-report-queue
      description: REST surface for assets-assetId-report-queue.
      operations:
      - method: POST
        name: post
        description: AssetQueueReportList
        call: ixon-assetqueuereportlist.post
        with:
          assetId: rest.assetId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-assetqueuereportlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AssetQueueReportList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: assetqueuereportlist
      description: AssetQueueReportList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-assetqueuereportlist.post
      with:
        assetId: tools.assetId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
