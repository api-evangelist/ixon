naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserVpnConfigList
  description: 'IXON API — MyUserVpnConfigList. 1 operations. Lead operation: MyUserVpnConfigList. Self-contained Naftiko
    capability covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserVpnConfigList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuservpnconfiglist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserVpnConfigList business capability. Self-contained, no shared references.
    resources:
    - name: users-me-vpn-configs
      path: /users/me/vpn-configs
      operations:
      - name: post
        method: POST
        description: MyUserVpnConfigList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuservpnconfiglist-rest
    port: 8080
    description: REST adapter for IXON API — MyUserVpnConfigList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/users/me/vpn-configs
      name: users-me-vpn-configs
      description: REST surface for users-me-vpn-configs.
      operations:
      - method: POST
        name: post
        description: MyUserVpnConfigList
        call: ixon-myuservpnconfiglist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuservpnconfiglist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserVpnConfigList. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myuservpnconfiglist
      description: MyUserVpnConfigList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-myuservpnconfiglist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
