naftiko: 1.0.0-alpha2
info:
  label: IXON API — MyUserPushDevice
  description: 'IXON API — MyUserPushDevice. 1 operations. Lead operation: MyUserPushDevice. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - MyUserPushDevice
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-myuserpushdevice
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — MyUserPushDevice business capability. Self-contained, no shared references.
    resources:
    - name: users-me-push-devices-publicId
      path: /users/me/push-devices/{publicId}
      operations:
      - name: get
        method: GET
        description: MyUserPushDevice
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-myuserpushdevice-rest
    port: 8080
    description: REST adapter for IXON API — MyUserPushDevice. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/me/push-devices/{publicid}
      name: users-me-push-devices-publicid
      description: REST surface for users-me-push-devices-publicId.
      operations:
      - method: GET
        name: get
        description: MyUserPushDevice
        call: ixon-myuserpushdevice.get
        with:
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-myuserpushdevice-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — MyUserPushDevice. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: myuserpushdevice
      description: MyUserPushDevice
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-myuserpushdevice.get
      with:
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
