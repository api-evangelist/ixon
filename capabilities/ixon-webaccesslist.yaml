naftiko: 1.0.0-alpha2
info:
  label: IXON API — WebAccessList
  description: 'IXON API — WebAccessList. 2 operations. Lead operation: WebAccessList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - WebAccessList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-webaccesslist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — WebAccessList business capability. Self-contained, no shared references.
    resources:
    - name: web-accesses
      path: /web-accesses
      operations:
      - name: get
        method: GET
        description: WebAccessList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: post
        method: POST
        description: WebAccessList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-webaccesslist-rest
    port: 8080
    description: REST adapter for IXON API — WebAccessList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/web-accesses
      name: web-accesses
      description: REST surface for web-accesses.
      operations:
      - method: GET
        name: get
        description: WebAccessList
        call: ixon-webaccesslist.get
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: post
        description: WebAccessList
        call: ixon-webaccesslist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-webaccesslist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — WebAccessList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: webaccesslist
      description: WebAccessList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-webaccesslist.get
      outputParameters:
      - type: object
        mapping: $.
    - name: webaccesslist-2
      description: WebAccessList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-webaccesslist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
