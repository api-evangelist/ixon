naftiko: 1.0.0-alpha2
info:
  label: IXON API — DataDeletionRequestList
  description: 'IXON API — DataDeletionRequestList. 1 operations. Lead operation: DataDeletionRequestList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - DataDeletionRequestList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-datadeletionrequestlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — DataDeletionRequestList business capability. Self-contained, no shared references.
    resources:
    - name: data-deletion-requests
      path: /data-deletion-requests
      operations:
      - name: post
        method: POST
        description: DataDeletionRequestList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-datadeletionrequestlist-rest
    port: 8080
    description: REST adapter for IXON API — DataDeletionRequestList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/data-deletion-requests
      name: data-deletion-requests
      description: REST surface for data-deletion-requests.
      operations:
      - method: POST
        name: post
        description: DataDeletionRequestList
        call: ixon-datadeletionrequestlist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-datadeletionrequestlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — DataDeletionRequestList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: datadeletionrequestlist
      description: DataDeletionRequestList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-datadeletionrequestlist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
