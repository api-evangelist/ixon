naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentListOpenVpnConnection
  description: 'IXON API — AgentListOpenVpnConnection. 1 operations. Lead operation: AgentListOpenVpnConnection. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentListOpenVpnConnection
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentlistopenvpnconnection
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentListOpenVpnConnection business capability. Self-contained, no shared references.
    resources:
    - name: agents-connection-openvpn
      path: /agents/connection/openvpn
      operations:
      - name: post
        method: POST
        description: AgentListOpenVpnConnection
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentlistopenvpnconnection-rest
    port: 8080
    description: REST adapter for IXON API — AgentListOpenVpnConnection. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/connection/openvpn
      name: agents-connection-openvpn
      description: REST surface for agents-connection-openvpn.
      operations:
      - method: POST
        name: post
        description: AgentListOpenVpnConnection
        call: ixon-agentlistopenvpnconnection.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentlistopenvpnconnection-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentListOpenVpnConnection. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentlistopenvpnconnection
      description: AgentListOpenVpnConnection
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentlistopenvpnconnection.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
