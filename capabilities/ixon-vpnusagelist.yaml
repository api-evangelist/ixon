naftiko: 1.0.0-alpha2
info:
  label: IXON API — VpnUsageList
  description: 'IXON API — VpnUsageList. 1 operations. Lead operation: VpnUsageList. Self-contained Naftiko capability covering
    one Ixon business surface.'
  tags:
  - Ixon
  - VpnUsageList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-vpnusagelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — VpnUsageList business capability. Self-contained, no shared references.
    resources:
    - name: vpn-usage-2
      path: /vpn-usage-2
      operations:
      - name: post
        method: POST
        description: VpnUsageList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-vpnusagelist-rest
    port: 8080
    description: REST adapter for IXON API — VpnUsageList. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/vpn-usage-2
      name: vpn-usage-2
      description: REST surface for vpn-usage-2.
      operations:
      - method: POST
        name: post
        description: VpnUsageList
        call: ixon-vpnusagelist.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-vpnusagelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — VpnUsageList. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: vpnusagelist
      description: VpnUsageList
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-vpnusagelist.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
