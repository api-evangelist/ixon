naftiko: 1.0.0-alpha2
info:
  label: IXON API — GroupPageImport
  description: 'IXON API — GroupPageImport. 1 operations. Lead operation: GroupPageImport. Self-contained Naftiko capability
    covering one Ixon business surface.'
  tags:
  - Ixon
  - GroupPageImport
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-grouppageimport
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — GroupPageImport business capability. Self-contained, no shared references.
    resources:
    - name: groups-publicId-page-import
      path: /groups/{publicId}/page-import
      operations:
      - name: post
        method: POST
        description: GroupPageImport
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: publicId
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-grouppageimport-rest
    port: 8080
    description: REST adapter for IXON API — GroupPageImport. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/groups/{publicid}/page-import
      name: groups-publicid-page-import
      description: REST surface for groups-publicId-page-import.
      operations:
      - method: POST
        name: post
        description: GroupPageImport
        call: ixon-grouppageimport.post
        with:
          publicId: rest.publicId
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-grouppageimport-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — GroupPageImport. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: grouppageimport
      description: GroupPageImport
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-grouppageimport.post
      with:
        publicId: tools.publicId
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
