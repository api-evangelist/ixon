naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentDataAlarmOccurrenceLatestList
  description: 'IXON API — AgentDataAlarmOccurrenceLatestList. 1 operations. Lead operation: AgentDataAlarmOccurrenceLatestList.
    Self-contained Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentDataAlarmOccurrenceLatestList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentdataalarmoccurrencelatestlist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentDataAlarmOccurrenceLatestList business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-latest-alarm-occurrences
      path: /agents/{agentId}/latest-alarm-occurrences
      operations:
      - name: get
        method: GET
        description: AgentDataAlarmOccurrenceLatestList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentdataalarmoccurrencelatestlist-rest
    port: 8080
    description: REST adapter for IXON API — AgentDataAlarmOccurrenceLatestList. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/latest-alarm-occurrences
      name: agents-agentid-latest-alarm-occurrences
      description: REST surface for agents-agentId-latest-alarm-occurrences.
      operations:
      - method: GET
        name: get
        description: AgentDataAlarmOccurrenceLatestList
        call: ixon-agentdataalarmoccurrencelatestlist.get
        with:
          agentId: rest.agentId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentdataalarmoccurrencelatestlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentDataAlarmOccurrenceLatestList. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: agentdataalarmoccurrencelatestlist
      description: AgentDataAlarmOccurrenceLatestList
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: ixon-agentdataalarmoccurrencelatestlist.get
      with:
        agentId: tools.agentId
      outputParameters:
      - type: object
        mapping: $.
