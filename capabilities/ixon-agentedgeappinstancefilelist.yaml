naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceFileList
  description: 'IXON API — AgentEdgeAppInstanceFileList. 1 operations. Lead operation: AgentEdgeAppInstanceFileList. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceFileList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstancefilelist
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceFileList business capability. Self-contained, no shared references.
    resources:
    - name: agents-edge-app-instances-files
      path: /agents/edge-app-instances/files
      operations:
      - name: delete
        method: DELETE
        description: AgentEdgeAppInstanceFileList
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstancefilelist-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceFileList. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/edge-app-instances/files
      name: agents-edge-app-instances-files
      description: REST surface for agents-edge-app-instances-files.
      operations:
      - method: DELETE
        name: delete
        description: AgentEdgeAppInstanceFileList
        call: ixon-agentedgeappinstancefilelist.delete
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstancefilelist-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceFileList. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentedgeappinstancefilelist
      description: AgentEdgeAppInstanceFileList
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: ixon-agentedgeappinstancefilelist.delete
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
