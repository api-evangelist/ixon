naftiko: 1.0.0-alpha2
info:
  label: IXON API — AgentEdgeAppInstanceDeploy
  description: 'IXON API — AgentEdgeAppInstanceDeploy. 1 operations. Lead operation: AgentEdgeAppInstanceDeploy. Self-contained
    Naftiko capability covering one Ixon business surface.'
  tags:
  - Ixon
  - AgentEdgeAppInstanceDeploy
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    IXON_API_KEY: IXON_API_KEY
capability:
  consumes:
  - type: http
    namespace: ixon-agentedgeappinstancedeploy
    baseUri: https://portal.ixon.cloud:443/api
    description: IXON API — AgentEdgeAppInstanceDeploy business capability. Self-contained, no shared references.
    resources:
    - name: agents-agentId-edge-app-instances-publicId-deploy
      path: /agents/{agentId}/edge-app-instances/{publicId}/deploy
      operations:
      - name: post
        method: POST
        description: AgentEdgeAppInstanceDeploy
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: agentId
          in: path
          type: string
          required: true
        - name: publicId
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.IXON_API_KEY}}'
  exposes:
  - type: rest
    namespace: ixon-agentedgeappinstancedeploy-rest
    port: 8080
    description: REST adapter for IXON API — AgentEdgeAppInstanceDeploy. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/agents/{agentid}/edge-app-instances/{publicid}/deploy
      name: agents-agentid-edge-app-instances-publicid-deploy
      description: REST surface for agents-agentId-edge-app-instances-publicId-deploy.
      operations:
      - method: POST
        name: post
        description: AgentEdgeAppInstanceDeploy
        call: ixon-agentedgeappinstancedeploy.post
        with:
          agentId: rest.agentId
          publicId: rest.publicId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: ixon-agentedgeappinstancedeploy-mcp
    port: 9090
    transport: http
    description: MCP adapter for IXON API — AgentEdgeAppInstanceDeploy. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: agentedgeappinstancedeploy
      description: AgentEdgeAppInstanceDeploy
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: ixon-agentedgeappinstancedeploy.post
      with:
        agentId: tools.agentId
        publicId: tools.publicId
      outputParameters:
      - type: object
        mapping: $.
